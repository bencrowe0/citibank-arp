# CLAUDE.md

Citibank Applied Research Project - earnings materials in, DeepSeek-powered structured BUY/HOLD/SELL predictions out, calibrated against actual stock price outcomes.

## Architecture

**6 issuers** (`jpm`, `bank_of_america`, `boeing`, `disney`, `target`, `netflix`) across 4 sectors - Financials (JPMorgan, Bank of America), Media/Communication Services (Netflix, Disney), Industrials (Boeing), Consumer/Retail (Target). Four scored layers, blended into one signal:

- **Micro** (`report_pipeline.py`, `run_reports.py`) - the company's own earnings materials (press release + presentation + transcript, bundled into one LLM call per quarter). This is the core layer; everything else adjusts it.
- **Macro** (`llm_macro.py`) - FOMC minutes, scored once per meeting and reused across every company/quarter whose `report_date` falls after that meeting's release (entity-agnostic, cached under `outputs/macro/results/`).
- **News** (`llm_news.py`) - a **pre-earnings market-expectations digest** per company/quarter (`REPORT_TYPE = "Pre-Earnings Market Expectations Digest"`), sourced from free outlets (CNBC, Yahoo Finance) via web search, stored as plain text under `docs/news/<issuer>/<document_id>.txt` with source URLs + publish dates recorded inline for auditability. **Every source article is dated strictly before `report_date`** - this is a hard rule, not a convenience: an earlier version of this layer used an "earnings date and following days" window and leaked actual post-earnings stock-reaction language into the digest text (see "Data leakage fix" below). The layer now scores how stretched or depressed market expectations were heading into the print, not sentiment about results that don't exist yet in the source text. Backfilled (2022-2024) quarters do not have a news digest (see Round 4) - this is a known gap, not a bug.
- **Quant** (`quant_layer.py`) - deterministic, yfinance-only, zero-LLM-cost composite of price momentum, EPS surprise, and a macro-numeric sub-component (`^TNX`, `^IRX`, `^VIX`). Added in Round 4; see below for why it doesn't yet earn blend weight.
- **Blend** (`blend.py`) - weighted sum of the four layer scores into one blended sentiment score, then the same `hold_upper`/`hold_lower` threshold mechanism derives BUY/HOLD/SELL. `DEFAULT_WEIGHTS = (0.8, 0.0, 0.2, 0.0)` (micro/macro/news/quant) as of Round 4 - this is the pooled-LOOCV-selected default, not a hand-tune (see Round 4).

Micro/macro/news share the same prompt (`prompts/llm_analysis_prompt_template.md`), just with a different `{{REPORT_TYPE}}` and `{{REPORT_TEXT}}`. Quant is pure arithmetic, no LLM call.

## Manifest schema

`manifests/<issuer>_reports.json` - each report entry has a `documents` list (not a single `source_pdf`): `[{"doc_type": "Press Release", "source_pdf": "..."}, ...]`. Single-document issuers (Netflix) just have a list of length 1; Disney backfilled quarters use press release + transcript (length 2, no presentation - Disney doesn't file one). `report_pipeline.build_bundle_text()` concatenates a report's documents into one labeled text block per quarter.

Long filings (10-K/10-Q, and for JPMC the earnings release financial supplement) are downloaded and stored under `docs/<issuer>/filings/` but are **intentionally excluded** from every layer's default input - too long/dense, matches the group's "too financy" feedback on the source documents.

## Dataset naming convention (canonical as of Round 5)

Every scored source document lives under **`docs/<issuer>/CY<FY>-Q<FQ>/`**, where `CY<FY>-Q<FQ>` is the **fiscal period being reported** (from the manifest `fiscal_period`, e.g. `FQ1 2026` -> `CY2026-Q1`), NOT the calendar quarter of the release date. Filenames stay issuer-native (each issuer has its own scheme, e.g. `jpm-1q25-press-release.pdf`, `2025-05-21-TGT-FQ1-2025-Transcript.pdf`) - only the folder is normalized. Excluded long filings go under `docs/<issuer>/filings/`. Result artifacts are `outputs/<issuer>/results/<TICKER>_FQ<Q>_<YEAR>.json`. Round 5 normalized the whole 2025 cohort (previously flat `docs/<issuer>/Q1..Q4/`), JPM's `2024-Q3` (missing `CY`), and Netflix's loose `docs/netflix/FQ2 25.pdf` files onto this scheme; any future backfill must follow it.

## Running the pipeline

See [README.md](README.md) for the micro-layer run commands. For the macro/news/blend/eval pieces:

```powershell
python llm_macro.py                         # score all FOMC minutes (cached, idempotent)
python llm_news.py                          # score all pre-fetched news digests (cached, idempotent)
python quant_layer.py                       # score all quarters' quant composite (free, cached, idempotent)
python blend.py <issuer>                    # blend micro+macro+news+quant for one issuer's quarters
python -m eval.run_eval                     # calibrate threshold + blend weights, pooled across ALL issuers
python -m eval.run_eval --issuers jpm,bank_of_america   # per-sector experiment (see Round 4)
python -m experiments.weight_threshold_sweep   # Round 5: fine grid + asymmetric thresholds + Fed ablation + significance test
python build_cost_ledger.py                 # rebuild outputs/global/summary/api_cost_ledger.csv from existing artifacts (no API calls)
python backtest.py                          # Round 8: P&L backtest of the LLM's overnight-gap trades, net of costs (equity curve + direction breakdown)
python backtest.py --sheet export.csv       # score every HUMAN rater from a group-sheet CSV export on the identical strategy
```

`run_reports.py` also takes `--prompt <file>` (test an alternate prompt template without touching the default), `--ensemble N` (average N repeat runs per document instead of trusting a single call), and `--variant <label>` (writes to `outputs/<issuer>_<label>/` instead of `outputs/<issuer>/`, so an experimental run can never overwrite the canonical one). `python -m eval.run_eval --issuers <issuer>_<label>,...` evaluates a variant folder the same way (macro/news lookups automatically resolve the variant label back to the real issuer name). `compare_runs.py <results_a> <results_b>` prints a side-by-side signal/score diff between any two results folders, optionally scored against a ground-truth CSV. See "Round 3" below for the workflow these were built for.

## Two distinct threshold concepts - do not conflate

- `hold_upper` / `hold_lower` - thresholds on the LLM's **sentiment score** (-1..+1), used to derive a prediction.
- `outcome_upper` / `outcome_lower` (`eval/outcomes.py`) - thresholds on the stock's **actual forward return**, used to derive the ground-truth label a prediction is checked against.

Never reuse the `hold_*` names for the outcome-side threshold or vice versa.

## Known limitations

- **N=131 pooled (Round 5), and now statistically significant.** `eval/run_eval.py` pools all 6 issuers before leave-one-out cross-validation so the tuned threshold/weights are one shared answer rather than six separately overfit ones. As of Round 5, pooled LOOCV default=tuned accuracy is **0.466** (threshold-only 0.45) against a majority-class (always-HOLD) baseline of **0.382**; a binomial test (p=0.030) and a 5,000-shuffle label-permutation test (p=0.0014) put this **significantly above noise** - the first round where "the signal is real" is a quantitative verdict, not an eyeballed point estimate (see `outputs/global/summary/weight_threshold_sweep.json`). The point estimate is *lower* than N=78's 0.54 (partly small-N optimism there, partly the backfilled 2021-2022 quarters carrying a thinner document set - press release ± presentation, no transcript/news); significance comes from the larger, more balanced sample beating a proper null, which is stronger evidence than a higher accuracy that wasn't significant. Still not large-N, but the most convincing version so far.
- **News layer source bias, and now a news coverage gap.** Digests are pulled from a small set of free outlets (CNBC, Yahoo Finance) in a fixed pre-report date window - auditable (source URLs + publish dates recorded) but not a guarantee against selection/outlet bias. Additionally, the 54 quarters backfilled in Round 4 (2022-2024) have **no news digest at all** - re-sourcing a leak-free, pre-earnings digest for 54 additional quarters was out of scope for the backfill session; `blend_scores`' missing-layer redistribution handles this automatically (those quarters blend on micro+quant only), but it means the news layer's sample is smaller and more recent (2025 quarters) than the micro/quant layers.
- **Blend beats micro alone by a modest margin.** Pooled LOOCV at N=131 (Round 5): threshold-only 0.45, blend 0.466 (both default=tuned). A finer grid (`WEIGHT_STEP=0.05`) plus the previously-unsearched **asymmetric-threshold** region (113,344 combinations) still lands the global best-fit on the exact default `(0.8, 0.0, 0.2, 0.0)` at `±0.25` - see `experiments/weight_threshold_sweep.py`.
- **Neither the quant layer, its Fed-data sub-component, nor macro earns blend weight.** At N=131 optimal quant and macro weight is 0.0; the Round 5 Fed-ablation (plain vs +macro-numeric vs +fred vs +macro+fred) gives byte-identical results because quant weight is 0. Real Fed data (FRED `DFF` + `T10Y2Y`, leakage-safe) was added and tested honestly - a stable negative, kept in the codebase, not removed. Matches the project's history with macro/news.
- **Per-sector calibration overfits, confirmed at larger per-sector N.** Round 4 re-ran per-sector LOOCV at N=13-26/sector (was N~4-8/sector in the original experiment): Financials (jpm+bac, N=26) shows a real gap (tuned 0.62 vs default 0.54 threshold-only) that could be a genuine sector effect or still overfitting at N=26; Industrials (boeing, N=13) and Consumer/Retail (target, N=13) both show tuning making things *worse* than default (0.31 vs 0.46, and 0.31 vs 0.54) - the textbook overfitting signature. Pooled remains the default methodology; per-sector stays an experiment.
- **Long filings excluded by design.** 10-K/10-Q filings and JPMC's financial supplement are stored but never fed to any layer. If a future phase wants deeper regulatory/financial-statement context, this is the obvious extension point.
- **Ground truth is a 5-day forward return by default; the group sheet's own metric is the overnight close-to-OPEN gap at ±2% (`Settings!B3`). On the overnight metric, raw accuracy cannot beat the majority baseline - balanced accuracy is the correct scorecard.** `eval/outcomes.py`'s default `±3%` close-to-close bucketing is calibrated for a 5-day move. The group sheet instead buckets the overnight gap `(next_day_open - report_date_close)/report_date_close` at `Settings!B3`=2% (see Round 7; `fetch_forward_return(exit_on_open=True)` reproduces it). At B3=2 the labels are ~69% FLAT, so the always-FLAT baseline is 0.687 and **raw accuracy provably cannot beat it** (needs predictor-gap correlation r≈0.45; strongest layer is r≈0.22) - not at any integer B3, not with any LOOCV-honest weight/threshold config. **Balanced accuracy** (mean per-class recall, fixed 1/3 floor regardless of skew) is the correct metric: a skew-aware LOOCV-tuned model scores **0.498 vs 0.333, p=0.0012 (significant)**. Do not chase raw-accuracy-beats-majority on a short/skewed window - it's a metric artifact, not a real ceiling. **Round 8 goes further: for a trading tool, raw accuracy is the wrong scorecard entirely - use `backtest.py`'s P&L metric, where the deployed model turns $1 into $2.73 net of costs on the overnight gap despite "failing" the accuracy test.**

## Prediction-accuracy fix (2026-07-02)

First pooled eval run scored **2/12 (16.7%)** - worse than naive always-SELL (50%) or always-HOLD (41.7%) baselines, with a systematic BUY-bias: 11/12 quarters predicted BUY regardless of actual outcome (actual distribution was 50% SELL / 41.7% HOLD / 8.3% BUY - 2025 was a rough year for Boeing/JPM/Netflix).

**Root cause, diagnosed against the actual data**: the prompt's scoring anchors (`prompts/llm_analysis_prompt_template.md`) mapped "solid results, modest beat, stable guidance" to +0.5 - but that description fits nearly every earnings release, so the micro layer (60% blend weight) floored almost every quarter above zero regardless of whether the stock was actually expected to move. The model was scoring "was this a good quarter in isolation" rather than "did forward guidance change relative to what the company itself had previously signaled." News (20% weight) compounded it; macro (20% weight, itself roughly balanced) was too small a weight to correct it.

**Fix applied**: rewrote the scoring-anchor section to make the score track *change in forward guidance versus the company's own prior guidance* rather than absolute quarter quality, with "in-line, modest beat, guidance reaffirmed" explicitly anchored to 0.0 instead of +0.5, plus two in-prompt calibration examples. Also reduced `eval/calibrate.py`'s `WEIGHT_STEP` from 0.1 to 0.2 (66 -> ~21 weight combinations) to shrink the joint search space's overfitting risk at N=12. All micro/macro/news scores were regenerated under the new prompt (old cached results deleted and re-run).

**Result**: prediction distribution is no longer degenerate (was 11 BUY / 0 HOLD / 1 SELL, now 4 BUY / 6 HOLD / 2 SELL - much closer to the actual 1/5/6 split). Pooled LOOCV: **default-weights accuracy 0.42** (was 0.17), **tuned blend-weight+threshold accuracy 0.58** (was 0.17, and previously tuning made things *worse* than default - 0.08 - which is no longer the case now that the underlying scores are properly calibrated). A supplementary run at a 10-trading-day outcome window (vs. the default 5-day) is saved alongside as `outputs/global/summary/global_outcome_calibration_window10.csv` / `global_calibration_summary_window10.json` - default accuracy is lower at that window (0.17) but tuned blend still recovers to 0.50; the 5-day window remains the default methodology, this is reported as a supplementary robustness check, not a replacement.

Still small-N (12 documents) - this is a real, meaningful improvement, not a fully solved problem, and should be presented to the team as directional evidence that fixing the LLM's scoring calibration (not just tuning thresholds after the fact) is what actually moves accuracy.

## Round 2: data leakage fix + further calibration (2026-07-02)

Asked to push accuracy further (target 8-9/12), with an explicit constraint: no shortcuts and no data leakage. Two things were found and fixed before any further tuning was attempted.

**Data leakage found and fixed.** The news digests (built under the round-1 fix above) were fetched from an "earnings date and following days" window. 4 of 12 digests turned out to contain explicit post-earnings stock-reaction language - e.g. "Boeing stock fell 4% on the announcement," "shares of JPM tumbled 1.9% after the release," "shares... declining by up to 12% following the announcement." The news layer (20% blend weight) was partially reading the actual outcome, not predicting it. Some of round 1's reported 7/12 (tuned blend) was leakage-inflated.

Fix: all 12 news digests were rebuilt from scratch using only articles dated **strictly before** `report_date` - pre-earnings analyst estimates, valuation framing, and expectations-setting coverage, sourced and date-verified per article. This redefines the news layer's purpose: it now measures how stretched or depressed market expectations were heading into the print, not sentiment about the print itself. A first pass at rescoring these under the *round-1* prompt scored everything ~0.0 (neutral) because the scoring anchors were built around "did guidance change," which doesn't apply to a document that predates the guidance - the model correctly noted "no guidance in this document" and defaulted to neutral, discarding real signal like "priced for perfection" valuation commentary. A second prompt fix added an explicit interpretation rule for pre-earnings digests: score how stretched (bearish) or depressed (bullish) expectations are, not whether guidance changed. After that fix, news scores became genuinely directional (e.g. all 4 Netflix quarters scored negative, correctly flagging the "stock priced for perfection" pattern the pre-earnings coverage kept surfacing).

**Micro-layer prompt further sharpened** (same file, extending round 1's anchors, not replacing them): explicit instructions to (a) read the earnings-call Q&A section for analyst pushback/management hedges as real signal, not just prepared remarks, (b) discount guidance improvements management itself attributes to FX/one-time/deferred items vs. organic performance, (c) weight structural/recurring risks (e.g. Boeing's 777X durability issue, KC-46A charge) above one-time noise, and (d) a new +0.15/+0.3 middle band for genuine operational momentum (e.g. a real loss-to-profit swing) that hasn't yet produced a formal guidance raise - the round-1 anchors were correctly catching the extremes but conflating "real momentum, no raise yet" with "flat quarter, no raise."

**Result, fully corrected for leakage**: pooled LOOCV lands at **6/12 (50%)**, achieved by the micro-layer signal alone (threshold-only prediction) - this is the current best, honestly-arrived-at number, down from round 1's leakage-tainted 7/12 but up from the original 2/12. The corrected blend (weights + threshold jointly tuned via the same LOOCV machinery, untouched) performs *worse* (3/12) - LOOCV folds kept picking a 100%-news weight that fit the calibration set but did not generalize to the held-out document, the textbook overfitting signature at this N. This is reported honestly rather than hidden: **macro and news do not currently add predictive value over micro alone**, even though the news layer is now genuinely informative and leak-free. `blend.py`'s `DEFAULT_WEIGHTS` (0.6/0.2/0.2) is left unchanged rather than hand-fit to this finding.

**Target of 8-9/12 was not reached.** Per the plan agreed before this round, prompt refinements were required to be justified by an articulable document-reading principle, not "whatever flips this specific row" - and weight/threshold tuning was restricted to the existing LOOCV machinery, not manual overrides. Both constraints were honored. The remaining wrong calls (BA_FQ2, BA_FQ4, NFLX_FQ1_2026, NFLX_FQ2, NFLX_FQ3, NFLX_FQ4) were individually reviewed; all six have document-defensible sentiment scores near the HOLD/BUY boundary that the model can't confidently push further one way without inventing a rule that wouldn't apply to any *other* document - i.e. this looks like the genuine ceiling for a document-grounded, non-leaking predictor at N=12, not a gap the team should expect to close with more prompt engineering on this same 12-document set. Growing N (as the rest of the team's companies land) is the most promising remaining lever.

## Round 3: evaluated a teammate's (Nigel's) fork for ideas (2026-07-02)

Nigel had amended Ben's first push - an earlier commit than round 1/2 above, before the multi-document bundling, BUY-bias fix, or leakage fix existed. Rather than merge his branch (which would have regressed the pipeline), his diff was mined for independently-good ideas, each evaluated on its own merits against the current 6/12 baseline before being adopted. Nothing was merged just because it "didn't hurt" - see the decision rule this round used, kept in `C:\Users\bencr\.claude\plans\nigel-ammended-the-first-ethereal-lerdorf.md`.

**Adopted (infra/correctness, no accuracy claim):** `report_pipeline.py` gained a deterministic `derive_signal()` recompute in `call_llm` - the stored `signal.direction` is now always derived from the score against the fixed thresholds rather than trusted verbatim from the model, with the model's own stated direction kept as `model_stated_direction` and any disagreement flagged as `signal_mismatch` (an audit signal, not something `eval/run_eval.py` depended on - it already recomputed the signal from the raw score independently, so this doesn't change any reported accuracy number). Also added: `validate_result()` (checks required JSON keys before accepting a response), retry-with-backoff in `call_llm`, `cache_hit_tokens()` (trusts DeepSeek's actual `prompt_cache_hit_tokens` over the previous cached-input guess), and a `PromptPack` class plus `--prompt`/`--ensemble`/`--variant` flags on `run_reports.py` so an experimental prompt or setting can be tested into its own `outputs/<issuer>_<variant>/` folder without touching the canonical one. `compare_runs.py` (new file) does side-by-side signal/score diffing between two results folders, optionally scored against a ground-truth CSV - this is what made the two tests below fast to run and audit.

**Tested and rejected: ensemble averaging.** Nigel's `ensemble_llm()` runs a document N times and predicts off the mean score, on the theory that averaging smooths run-to-run noise on boundary documents (round 2 had flagged all 6 remaining wrong calls as boundary cases). Tested with `--ensemble 3` on all 12 documents under the unchanged round-2 prompt (`outputs/<issuer>_ensemble3/`). Result: pooled LOOCV threshold-only accuracy **dropped to 0.33 (4/12)**, from baseline 0.50 (6/12). Per-document diff (`compare_runs.py`, then cross-checked against `eval/outcomes.py`'s independent score-vs-threshold recompute): the 3-run mean nudged three already-near-the-0.15-boundary scores *upward* - JPM_FQ2 (0.15→0.167), JPM_FQ3 (0.15→0.183), NFLX_FQ1_2026 (0.15→0.2) - flipping two previously-correct HOLD calls (JPM_FQ2, JPM_FQ3) into wrong BUYs, with no compensating correct flips anywhere else. The nudges were consistently upward across every affected document, not randomly distributed, which argues against "just noise cancelling out" being a reliable mechanism at this N and this close to the threshold. **Rejected** - `ensemble_llm`/`--ensemble` stay in the codebase as a tested, available option (e.g. for future prompts that aren't sitting exactly on the boundary), but are not used in the default run.

**Tested and rejected: "surprise vs. expectations" prompt reframing.** Nigel's separate `llm_reaction_prompt_v2.md` draft scores the document as a forecast of surprise-driven market reaction rather than "change in the company's own forward guidance." A merged version (`prompts/llm_reaction_prompt_v2_merged.md`) was written combining that reframing with the round-2 anchors (organic-vs-attributed, Q&A pushback, structural-vs-one-time risk, pre-earnings-digest handling, all kept unchanged) rather than testing nigel's file verbatim, since those round-2 refinements are compatible with a surprise-based framing, not a replacement for it. Tested single-run (no ensembling) on all 12 documents (`outputs/<issuer>_v2prompt/`). Result: pooled LOOCV threshold-only accuracy **dropped to 0.33 (4/12)**. Per-document diff showed a systematic *upward* score shift versus the round-2 prompt on almost every document (e.g. BA_FQ2 0.25→0.30, JPM_FQ2/JPM_FQ3 0.15→0.20, NFLX_FQ1_2026 0.15→0.25) - the same two JPM boundary docs flipped wrong, plus NFLX_FQ1_2026 also flipped HOLD→BUY (wrong; actual outcome SELL), with no new correct calls anywhere. In effect, despite v2's own explicit anti-BUY-bias language ("in-line documents should score near zero even when results are strong"), the surprise framing combined with round-2's anchors still produced a mild version of the exact BUY-bias round 1 had to fix. **Rejected** - the merged prompt file is left in the repo as a tested, documented dead end (in case a future prompt iteration wants to revisit the surprise framing with a different anchor calibration), but the default prompt (`prompts/llm_analysis_prompt_template.md`) is unchanged.

**Net result: the round-2 baseline (6/12, micro-only threshold prediction) is still the best result and is unchanged.** Both scoring-affecting ideas from Nigel's fork were tested honestly against the pooled-LOOCV harness and both underperformed with no tie-breaking argument in their favor, so per this round's decision rule neither was adopted. The infra improvements (signal-mismatch auditing, retry/validation robustness, accurate cache-hit cost accounting, and the `--prompt`/`--ensemble`/`--variant`/`compare_runs.py` A/B-testing toolchain) are kept regardless, since they were justified on correctness/tooling grounds independent of this outcome. Full experiment artifacts (extra output folders, saved eval summaries) are kept under `outputs/*_ensemble3/`, `outputs/*_v2prompt/`, and `outputs/global/summary_comparisons/` for audit.

## Round 4: quant layer, backfill to N=78, per-sector re-check, cost ledger (2026-07-08)

Four things were requested this session: a new free quantitative layer, scaling N by backfilling earlier quarters, a per-sector vs. pooled re-check, and API cost tracking - all without overfitting or leakage. `bank_of_america` was added as a 6th issuer alongside the existing 5 (jpm, boeing, netflix, disney, target), matching CLAUDE.md's already-updated (but previously undocumented) issuer count.

**Quant layer added** (`quant_layer.py`, new file): deterministic, yfinance-only, zero-LLM-cost. Composite of price momentum (trailing return into the print), EPS surprise (from `get_earnings_dates`), and a macro-numeric sub-component (`^TNX`, `^IRX`, `^VIX` levels as of `report_date`) - all collapsed into **one** `quant` score via a fixed formula (not one tunable weight per metric), so it adds exactly one blend dimension, same overfitting-control discipline as macro/news. Wired into `blend.py`/`eval/calibrate.py`/`eval/run_eval.py` as a 4th layer with default weight 0.0 (verified byte-for-byte identical blend behavior when quant weight is 0).

**Backfill: JPM + 5 more issuers, 2022-2024, N 24 -> 78.** Two prior sessions had failed to backfill via background subagents (misread the task once, then hit the weekly usage limit after spawning uncontrolled child agents against explicit instructions) - this round's backfill was done **inline in the main thread**, no subagents, matching the lesson learned. For each issuer, 9 quarters (FQ4 2022 through FQ4 2024) were added the same way: real report dates from `yfinance.get_earnings_dates()`, official sources found via web search (SEC EDGAR 8-K exhibits for press releases/presentations where filed, company IR CDN PDFs for presentations/transcripts where SEC doesn't carry them, Motley Fool/Insider Monkey for call transcripts as a free fallback when a company doesn't file one), every document downloaded and verified with `report_pipeline.extract_doc_text()` (must extract AND contain the correct year token - nothing was fabricated or accepted unverified), then scored via a temporary issuer-only manifest and merged into the canonical `manifests/<issuer>_reports.json`.

Backfilled per issuer: JPM (prior session, 9 quarters, $0.087), Bank of America (9 quarters, $0.165), Boeing (9 quarters, $0.081), Disney (9 quarters, $0.084), Target (9 quarters, $0.089), Netflix (9 quarters, $0.052). Total backfill LLM cost this session: **~$0.47**. Verification caught two real mistakes before they entered the manifest: a Boeing SEC accession that turned out to be a CEO-succession 8-K rather than the earnings 8-K (wrong document, same filing date - caught because the extracted text read "Boeing Board Names Kelly Ortberg..." instead of "Boeing Reports..."), and a Target stray-download folder-naming scheme left off-by-one by an earlier failed agent run (fixed by renaming to match the fiscal-period-based convention used elsewhere, not the report date's calendar quarter).

Backfilled quarters get **micro + quant only** (no news digest - re-sourcing 54 leak-free pre-earnings digests was out of scope this round; `blend_scores`' existing missing-layer redistribution handles this automatically) and **no macro** for dates the existing FOMC manifest doesn't cover. Disney's backfilled quarters use press release + transcript (2 docs) rather than the 2025-quarters' transcript-only pattern, since Disney's SEC 8-K exhibits include an official press release that adds real grounding - documented as an intentional, not accidental, inconsistency in per-quarter document composition (the manifest schema already supports variable-length `documents` lists per quarter).

**Result at N=78**: pooled LOOCV threshold-only **default=tuned=0.51** (was 0.55 default at N=33, and diverging from tuned at smaller N); blend **default=tuned=0.54**. The overfitting signature from every earlier round (tuned beating default on the calibration set, then failing to generalize) has disappeared: inspecting all 78 LOOCV folds' tuned weights individually shows **every single fold independently selects the exact hardcoded default (0.8/0.0/0.2/0.0)** - not a coincidence at this point, a genuine convergence. Quant still does not earn weight (same as macro), consistent with the project's honest history of macro/news not adding measurable value at these sample sizes - reported as a stable negative, not hidden.

**Per-sector re-check at larger per-sector N** (still an experiment, pooled remains default): Financials (jpm, bank_of_america; N=26) - threshold tuned 0.62 vs default 0.54, blend tuned 0.62 vs default 0.58 - a real gap that could be a genuine sector effect at N=26 or still overfitting; worth re-checking as Financials grows further. Media/Communication Services (netflix, disney; N=26) - threshold and blend both converged (tuned=default=0.50 and 0.54 respectively) - no overfitting gap, but no lift either. Industrials (boeing; N=13) and Consumer/Retail (target; N=13) both show tuning actively *hurting* (0.31 tuned vs 0.46/0.54 default) - the same overfitting signature the pooled default is designed to avoid, now visible at a larger N than the original per-sector experiment.

**Cost ledger built** (`build_cost_ledger.py`, new file): aggregates the per-call cost logs `run_reports.py`/`llm_macro.py`/`llm_news.py` already wrote into every result artifact, into one CSV (`outputs/global/summary/api_cost_ledger.csv`). Deliberately built as a **read-only aggregation script**, not a live per-call hook wired into the three scoring scripts - re-running it after any future scoring run picks up new cost data with zero additional API calls or code-path risk, which was judged the better tradeoff for a research pipeline over adding a shared `record_cost()` call site to three files for a modest interface convenience gain. Total logged cost across all layers to date: **~$0.68** (micro $0.557, news $0.065, macro $0.055; quant is free and not logged).

**`data/backfill_provenance.csv` filled** (122 rows: one per backfilled document, across all 6 issuers) with `source_url`, verification `status`, and a note confirming the extract-and-year-token check. A few rows note "exact URL not logged that session" for documents downloaded by earlier stray/failed-agent attempts before this round's URL-tracking discipline was in place, rather than fabricating a URL after the fact.

**Finer-grained pooled weight/threshold search (same session, follow-up ask).** The coarse production grid (`WEIGHT_STEP=0.2`, 6 threshold candidates, 336 total combos) was re-checked with a much finer exploratory grid (`WEIGHT_STEP=0.05`, 18 threshold candidates spanning 0.05-0.40, 31,878 total combos - not committed to `eval/calibrate.py`, run as a standalone script to avoid raising the production overfitting-risk surface). Two results: (1) the **global best-fit** (fit on all 78 documents, no held-out fold - the literal "best possible on this data") is accuracy **0.5385 (42/78)**, achieved by weights **(0.8, 0.0, 0.2, 0.0)** at threshold **±0.25** - exactly the existing hardcoded default (2 other combos tie the same accuracy, both still macro=quant=0, both losing the tie-break to "closest to default"). (2) **LOOCV at that same finer grid** gives tuned=default=**0.5385 across all 78 folds**, unanimous. This confirms the coarse-grid convergence reported above isn't a grid-resolution artifact - searching ~95x more combinations lands on the identical answer. Macro and quant contributing exactly zero weight is a real property of this data at N=78, not a search-resolution limitation; the only remaining lever to move this number is more data or a different scoring approach, not finer tuning.

**Not done this round** (explicitly deferred, not forgotten): re-sourcing news digests for the 54 backfilled quarters; a `data/quantitative/` replication-friendly folder layout for cached yfinance pulls (quant_layer.py currently re-fetches from yfinance each run rather than caching to disk). All are natural next steps if N keeps growing.

## Round 5: scale to N=131, Fed data layer, full weight/threshold sweep + significance test, dataset normalization (2026-07-09)

Four goals this session, all four addressed: scale data until predictive power is *statistically* real (not just a higher point estimate), add real Federal Reserve data, fully explore weight/threshold combinations for the pooled predictor, and get the dataset consistently organized. Done inline in the main thread (no subagents for the backfill, per the standing lesson).

**Data scaled N 78 -> 131 (backfill to 2021 + 6 gap quarters).** Preferring earlier quarters of existing issuers over new issuers (as asked), each issuer got its 2021-2022 quarters plus a missing recent gap quarter: JPM/Boeing/Bank of America +9, Target/Netflix +9, Disney +8 (Disney's 2022-11-08 was already present). Every backfilled document is an **official SEC EDGAR 8-K exhibit** found via EDGAR full-text search (`efts.sec.gov`), classified to the press-release exhibit (EX-99.1, or the single generic EX-99 some issuers file) plus the earnings presentation (EX-99.2) where cleanly identifiable, downloaded, and **verified by re-extraction + fiscal-year-token check before acceptance** - same no-fabrication discipline as Round 4. Composition is **press release + presentation** for JPM/Bank of America and **press release only** for Boeing/Disney/Target/Netflix (no transcript, no news digest for backfilled quarters - `blend_scores`' missing-layer redistribution handles this). This is a **thinner document set than the 2023-2025 quarters** (which have transcripts and, for 2025, news digests) - a documented composition difference, relevant to the accuracy discussion below. Provenance for all 71 backfilled documents is appended to `data/backfill_provenance.csv`. The one-shot SEC sourcer lives in the session scratchpad, not committed (throwaway tool, like Round 4's backfill scripts).

**Federal Reserve data layer added** (`quant_layer.py:compute_fred_macro`). Real Fed data pulled **key-free** from FRED's `fredgraph.csv` endpoint (keeps the layer's "free, no API key" property - no `fredapi` dependency, no `FRED_API_KEY` needed), cached to `data/quantitative/fred_cache/`. Two **non-redundant** signals: trailing federal-funds-rate change (`DFF`; rising policy rate -> tightening headwind, negative prior) and 10Y-2Y yield-curve slope (`T10Y2Y`; inversion -> risk-off, negative prior). Deliberately **excludes `DGS10`** (it just duplicates the `^TNX` already in `macro_numeric`). Leakage-safe: FRED publishes ~1 business day late, so it takes the last observation **strictly before** `report_date` (stricter than `macro_numeric`'s on/before, which is fine for intraday market quotes but not next-day-released Fed data). Signs come from stated economic priors, scales are fixed, nothing fit to outcomes - the same discipline as the rest of the quant layer. Exposed as toggleable ablation composites (`quant_score_with_fred`, `quant_score_with_macro_fred`) kept OUT of the headline `quant_score`; whether it earns weight is decided by eval, not asserted.

**Comprehensive weight/threshold sweep, now committed** (`experiments/weight_threshold_sweep.py`, run `python -m experiments.weight_threshold_sweep`). Round 4's finer grid was a throwaway; this is the committed, reproducible successor, kept OUT of `eval/calibrate.py` so the production overfitting surface stays small. It adds two things production never searched: a **finer weight grid** (`WEIGHT_STEP=0.05`) and, crucially, **asymmetric thresholds** - production only ever searched `hold_upper == -hold_lower`; this searches them independently (8x8 grid). 1,771 weight combos x 64 threshold pairs = **113,344 combinations per quant variant**, vectorized with numpy (a `[combo x doc]` correctness tensor makes full LOOCV cheap at N=131). Result: the **global best-fit is still exactly `(0.8, 0.0, 0.2, 0.0)` at symmetric `±0.25`** (accuracy 0.4656, 61/131), and LOOCV tuned (0.458) == default (0.4656). Searching ~340x more configurations than production, *including* the previously-unexplored asymmetric-threshold region, lands back on the hardcoded default - the strongest evidence yet that the default is not an overfit or a grid-resolution artifact.

**Fed layer (and macro-numeric) do not earn blend weight - a confirmed negative.** The sweep re-runs the entire optimization with each quant variant (plain / +macro-numeric / +fred / +macro+fred); **all four give identical results** because optimal quant weight is 0, so the quant variant is irrelevant to the blend. Real Fed data was added and rigorously tested, and it does not improve pooled prediction - the honest, publishable answer to "look at Fed reserves," consistent with the project's history of macro/news/quant not earning weight at these sample sizes.

**Predictive power is now statistically distinguishable from noise - the headline result.** Accuracy at N=131 is **0.466** (default model, pooled LOOCV) against a **majority-class (always-HOLD) baseline of 0.382** (the 131 outcomes are 39 SELL / 50 HOLD / 42 BUY - notably more balanced than N=78's HOLD-heavy split, because the 2021-2022 quarters carry more real up/down moves). Significance tests (in `weight_threshold_sweep.py`, output `outputs/global/summary/weight_threshold_sweep.json`): **binomial p=0.030** vs the majority baseline, and a **label-permutation test p=0.0014** (5,000 shuffles; only 0.14% of random label alignments matched the model's) -> **significant at N=131**. This is the first version of the project where "the signal is real, not noise" is a *quantitative verdict*, not an eyeballed point estimate.

**Important honesty caveat on the accuracy number.** The point estimate *fell* from N=78's 0.54 to N=131's 0.466. Two non-exclusive reasons: (a) the 0.54 at N=78 was partly small-N optimism, and (b) the backfilled quarters have a thinner document set (press release ± presentation, no transcript/news) so carry less signal than the 2023-2025 quarters. The reason the *weaker-looking* 0.466 is nonetheless the more convincing result is that significance is driven by N and by a proper null (permutation), not by the headline number - a larger, more balanced sample beating a proper baseline with p<0.05 is stronger evidence than a higher accuracy on a smaller, HOLD-skewed sample that wasn't significant. Do not present 0.466 as "worse than before" without this context.

**Dataset normalized to one convention** (see "Dataset naming convention" above). The whole 2025 cohort (previously flat `docs/<issuer>/Q1..Q4/`), JPM's `2024-Q3` (missing `CY`), and Netflix's loose `docs/netflix/FQ2 25.pdf` files were moved to canonical `docs/<issuer>/CY<FY>-Q<FQ>/`; 174 documents now conform (verified: 0 nonconforming, 0 missing on disk, all extract). JPM earnings supplements moved to `docs/jpm/filings/`. `manifests/macro_fomc_2025.json` renamed to `macro_fomc.json` (it already covers through 2026-04). yfinance price/earnings pulls now **cached to disk** (`data/quantitative/{price,earnings,fred}_cache/`, CSV, git-ignored) - the deferred Round 4 item; repeated quant/eval runs over the same quarters do zero redundant network calls (cold 2.0s -> warm 0.02s per fetch). Repo hygiene: `scratch_check_pdfs.py` removed, `nigels version/` + `notes and ideas.txt` moved to git-ignored `_archive/`, `OneDrive_2026-07-06/` (teammate syncs) git-ignored.

**Not done this round** (deferred, not forgotten): news digests for the 54+ backfilled quarters (still no news layer pre-2025); transcripts for backfilled quarters (SEC doesn't carry them; would need free-transcript sourcing like Round 4's fallback); the cost ledger's per-issuer micro totals are now dominated by the backfill run's summary CSV (the read-only aggregator reads the latest `<issuer>_first_run_summary.csv`, which the temp-manifest backfill overwrote) - a cosmetic accounting quirk, not a scoring issue.

## Round 6: overnight-direction ground truth + balanced-accuracy metric (2026-07-13)

Question that started this round: the ground truth (`eval/outcomes.py`) is a **5-day** close-to-close forward return, not the **overnight** market reaction to the print. How much does that change the Round 5 results, and can the pooled model be judged on overnight direction instead?

**5-day is a documented, intentional choice, not an oversight.** `eval/outcomes.py`'s docstring calls entry-price selection (next trading-day close on/after `report_date`) "conservative" - it avoids assuming a same-day intraday reaction is fully captured, since some reports release after market close and the manifests carry no BMO/AMC timing field. True overnight (close-before-release -> open-after-release) would need that timing sourced for all 131 quarters; not attempted this round. Instead, **overnight was approximated as the 1-trading-day close-to-close return** (`window_trading_days=1`, already a plumbed-through parameter on `eval/outcomes.fetch_forward_return`), which needs no new data and no BMO/AMC ambiguity.

**First attempt (raw accuracy) looked like a regression, and was diagnosed as a metric problem, not a real one.** At `window_trading_days=1`, the existing `±3%` outcome threshold (calibrated for 5-day moves) almost never trips, so the label distribution collapses from Round 5's 39/50/42 (SELL/HOLD/BUY) split to **~69% HOLD**. Raw accuracy of the untouched, 5-day-optimized default model dropped to 0.626 - which sounds close to Round 5's 0.466, but is actually **worse than the trivial always-HOLD baseline (0.687)** at this new skew. A full LOOCV-honest re-search of the pooled weight/threshold space (113,344 combinations, same machinery as Round 5's sweep, including the Fed/macro-numeric quant variants) topped out at 0.672 - still below 0.687. Read naively, this said "no overnight signal exists." That conclusion was wrong - the metric, not the model, was broken: raw accuracy's majority-class floor moves with label skew (0.382 at 5-day vs 0.687 at 1-day), so a raw-accuracy number from one window isn't comparable to another, and a model that just leans HOLD-heavy scores well without adding real information.

**Fix: added balanced accuracy (mean per-class recall across SELL/HOLD/BUY) as a selectable metric in `experiments/weight_threshold_sweep.py`** (`--optimize-metric {accuracy,balanced_accuracy}`, default `accuracy` so the committed 5-day artifact is untouched - verified byte-identical apart from additive fields on rerun). Its majority-only-classifier floor is a fixed **1/3 (0.333)** regardless of label skew - unlike raw accuracy's floor, it's comparable across windows and directly answers "is the model actually catching BUY/SELL, or just defaulting HOLD." Vectorized the same way as the rest of the sweep: a one-hot label matrix turns per-combo and per-LOOCV-fold balanced accuracy into two matrix products, so the cost stays negligible even across the full 113k-combo grid. New CLI also exposes `--window-trading-days` / `--outcome-upper` / `--outcome-lower` directly on the sweep script (previously hardcoded).

**Result: the existing (5-day-optimized, untouched) default model already clears the balanced-accuracy floor on overnight direction.** Scored on 1-day labels without any retuning: **balanced accuracy 0.504** vs the 0.333 majority-only floor, **significant** (5,000-shuffle label-permutation test, p=0.0004; see `outputs/global/summary/weight_threshold_sweep_window1_bal.json`). Re-running the same significance check on the 5-day data confirms consistency: balanced accuracy 0.4407 vs 0.333 floor, p=0.0008 (`outputs/global/summary/weight_threshold_sweep.json`, now carries both metrics).

**Retuning weights specifically for overnight makes it worse - same overfitting signature as Round 2.** A full LOOCV-honest re-search that selects combos by balanced accuracy on the training folds gets **0.4455** held-out - *below* the untouched default's 0.504. The no-holdout global best-fit (0.5122, heavy news weight 0.75) is the usual optimistic upper bound and isn't trustworthy on its own. **Recommendation, followed: do not migrate/retune the deployed weights for overnight prediction.** The 5-day-optimized `(0.8, 0.0, 0.2, 0.0)` default generalizes to the overnight-direction task as-is; searching a separate overnight-specific configuration only overfits at this N.

**Explicitly not done**: `outcome_upper`/`outcome_lower` (the ground-truth bucketing thresholds) were **not** retuned to rebalance the overnight label distribution - doing so would fit the label definition itself to the model, a worse form of leakage than tuning `hold_upper`/`hold_lower` ever was. True close-to-open gap accounting (vs. the 1-day close-to-close approximation used here) remains unimplemented, blocked on sourcing BMO/AMC release timing for all 131 quarters.

## Round 7: group sheet's TRUE overnight-gap ground truth + skew-aware balanced-accuracy win (2026-07-13)

Follow-up to Round 6 with the group's **actual** ground-truth definition, surfaced from the shared Google Sheet's direction formula: `=IF(...,IF(L>Settings!$B$3/100,"UP",IF(L<-Settings!$B$3/100,"DOWN","FLAT")))`. Two things this pins down that Round 6 approximated:

1. **The overnight move is a close-to-OPEN gap**, not close-to-close: `Actual % Change = (Next Day Open - Prior Close) / Prior Close`, where `Prior Close` = Close on `report_date` and `Next Day Open` = Open the next trading session (exactly `export_sheet_rows.fetch_prices`). Round 6's blocker ("needs BMO/AMC timing") is **dissolved** - the sheet just uses report-date Close -> next-day Open for every issuer (documented caveat for pre-market reporters JPM/BAC/TGT, whose report-date Close is already post-announcement). Implemented as a **first-class option**: `eval/outcomes.fetch_forward_return(..., exit_on_open=True)` (exit uses the session's Open instead of Close), threaded through `collect_outcomes_for_issuer` -> `eval/run_eval.build_documents_for_issuer` -> `experiments/weight_threshold_sweep.load_pooled`, exposed as `--exit-on-open` on the sweep. With `--window-trading-days 1 --outcome-upper 0.02 --outcome-lower -0.02` this reproduces the sheet's ground truth exactly.
2. **The direction threshold is `Settings!B3` = 2% (integer-tunable), not a derived value.** At B3=2 the overnight-gap labels are 19 SELL / 90 HOLD(FLAT) / 22 BUY -> **always-FLAT majority baseline = 0.687**.

**Raw accuracy CANNOT beat the always-FLAT baseline at B3=2 - proven, not eyeballed.** Confirmed four independent ways: deployed-default 0.550; production-grid LOOCV 0.634; widened-skew-grid (thresholds to +/-0.9, asymmetric) LOOCV 0.618; and the **no-holdout global best-fit only 0.657-0.702** (barely over baseline, and it fails to generalize). The clinching argument is a signal-strength analysis: for the actual gap distribution, an in-sample-optimal 3-class predictor needs Pearson correlation **r ~= 0.45-0.5 with the gap to clear 0.687 with any LOOCV-durable margin**, but the strongest layer is only **r ~= 0.22** (micro +0.218, news +0.217 on its 24 quarters, macro -0.061, quant composite +0.018 - both noise). Combining micro+news realistically reaches ~0.30 -> ~0.684 in-sample, still under. **No integer B3 yields an honest LOOCV win either** (B3=1: 0.44 vs 0.47 baseline; B3=3: 0.71 vs 0.79). This is a property of a weak-but-real signal on a heavily FLAT-skewed 3-class problem, not a fixable modelling gap - and forcing a win would require either tuning B3 to the model (fitting the label definition = worse leakage, refused) or an in-sample config that fails holdout (refused).

**The legitimate win: balanced accuracy (skew-robust) with a skew-aware decision rule.** Chosen with the user as the correct response to heavy class skew (its floor is a fixed 1/3 regardless of skew, unlike raw accuracy's baseline which climbs with the FLAT band). On the group's exact overnight-gap ground truth at B3=2, a **skew-aware model** - LOOCV-honest, pooled across all 6 issuers, weights+asymmetric-thresholds selected per fold by balanced accuracy on the *other* 130 docs, tie-broken to default - achieves **balanced accuracy 0.498 vs the 0.333 majority-only floor, permutation p=0.0012 (5,000 shuffles) -> significant** (`outputs/global/summary/weight_threshold_sweep_window1_gap_bal.json`, `significance_balanced` block). The selected weights are **news-heavy** (~0.7 news), independently confirming the theory that the overnight gap is a surprise-vs-expectations phenomenon the news/expectations layer captures, not a guidance-change phenomenon the micro layer was tuned for.

**Honesty caveats, all reported not hidden:** (a) raw accuracy stays **0.496 < 0.687** - the skew-aware model deliberately trades majority-class (FLAT) accuracy for minority-class (UP/DOWN) recall, predicting 33/55/43 vs the true 19/90/22; this is the correct move *for balanced accuracy* and the wrong move for raw accuracy, which is exactly why the two metrics diverge here. (b) The **deployed default** model's balanced accuracy on the gap is only **0.400 (p=0.068, marginal, NOT significant at 0.05)** - it is the *skew-aware LOOCV tuning* that reaches significance, and unlike the raw-accuracy retuning in Rounds 2/6 this one **generalizes under LOOCV** (0.498 held-out, close to the 0.516 in-sample best), because optimizing balanced accuracy on a skewed set is a better-posed objective than optimizing raw accuracy. (c) Fed/macro-numeric quant variants still change nothing (optimal quant weight 0). The committed sweep now emits both the default- and LOOCV-tuned-model balanced-accuracy significance (`correctness_tensor` also returns predictions; `evaluate_variant` returns `loocv_tuned_pred`; the tuned predictions are permutation-tested, valid because each is chosen on the other 130 docs).

**Round 6's close-to-close artifacts are superseded** by the close-to-open gap version for the "overnight" question: `weight_threshold_sweep_window1_gap_bal.json` is canonical; `weight_threshold_sweep_window1.json` / `_window1_bal.json` (close-to-close approximation) are left on disk as the earlier approximation, not the group's real ground truth. The 5-day production artifact (`weight_threshold_sweep.json`) is unchanged in every pre-existing number (regenerated additively with the new significance fields; verified +29 insertions, 0 deletions).

## Round 8: from accuracy to P&L - the backtest metric (2026-07-18)

The stakeholder framing this round: the tool is for Citibank, who "need good accuracy, not good balanced accuracy" - i.e. beat the always-FLAT baseline on **raw** accuracy on the group's overnight close-to-open gap. Rounds 6-7 had shown raw accuracy provably cannot beat always-FLAT overnight (0.687 baseline, needs r~=0.45, strongest layer r~=0.22). This round asked whether that verdict holds across all thresholds, and - crucially - whether raw accuracy is even the right scorecard for a **trading** tool. It is not.

**Confirmed the accuracy ceiling across every threshold and window (`b3_window_sweep.py`, throwaway diagnostic, not committed).** Swept B3 (the outcome-label threshold) in {1, 1.5, 2, 2.5, 3}% x {overnight-gap, 5-day} on the current signal, LOOCV-honest, without touching any committed artifact. Result: **overnight gap is dead at every threshold** - default and tuned both lose to the always-majority baseline at every B3 (binom p 0.83->1.0). No threshold rescues it. The **5-day window wins**: default-model raw accuracy beats its always-FLAT baseline AND is significant at +/-2.5% (0.435 vs 0.351, binom p=0.029) and at the pipeline's own default +/-3% (0.466 vs 0.382, p=0.031) - robust across two adjacent thresholds, so not a cherry-pick, and the **default beats tuned everywhere** (no overfitting needed). So the honest accuracy story: the tool already beats baseline on the 5-day horizon; overnight is unbeatable at any threshold; and the ~107-quarter news backfill (Phase 1 of the approved plan, estimated ~0.684) would not clear the 0.687 overnight baseline either. The backfill was started (one leak-free digest, `docs/news/jpm/JPM_FQ4_2020.txt`) then **deprioritized** once this diagnostic showed it cannot change the overnight verdict - the 107-quarter gap remains open, now with a quantified reason it is not the lever for the overnight goal.

**The reframe: accuracy is the wrong scorecard for a trading signal.** 3-class accuracy charges a *bet-on-a-flat* exactly the same penalty as a *bet in the wrong direction*. In P&L those are nothing alike - a flat bet is ~breakeven, a wrong-direction bet is a real loss. A short trade simulation (`overnight_pnl.py`, throwaway) confirmed the deployed model's overnight "errors" are almost all harmless: of its 44 directional calls it gets direction **backwards only 4 times**; 29 of the "wrong" calls are bets on stocks that then stayed flat (~breakeven).

**`backtest.py` (new, committed): predictor-agnostic P&L backtest.** Models the overnight-gap strategy (BUY->long into report-date close, exit next open; SELL->short; HOLD->no trade), P&L per trade = position * overnight gap - costs. Predictor-agnostic by design: `llm_predictions()` scores the deployed LLM from `global_outcome_calibration.csv`; `load_sheet_predictions(path)` ingests any CSV/TSV in the **group-sheet schema** (`Rater, Type (Human/LM), Ticker, Year, Quarter, Decision, Prior Close ($), Next Day Open ($)`) so every human rater is scored on the *identical* strategy/costs and compared apples-to-apples. Configurable transaction cost (`--cost-bps`, default 10 round-trip) + short-borrow (`--short-borrow-bps`). Reports total return (compounded), avg/trade, hit rate, Sharpe/trade, max drawdown, a correct/flat/wrong direction breakdown (labelled at the group's +/-2% band, for diagnosis only - never for P&L), and writes the per-trade equity curve to `outputs/global/summary/backtest_equity.csv`.

**Result (deployed LLM, overnight, net of 10 bps round-trip):** 44 trades of 131 prints, hit rate 59%, **total return +173%** ($1 -> $2.73 compounded), **+2.5%/trade**, Sharpe/trade 2.48, max drawdown 6.5%. Direction breakdown: 11 correct-direction (+127%), 29 bet-on-a-flat (+2%, ~wash), **only 4 wrong-direction (-15%)**. Baselines on the same set: **always-long LOSES** (-16%, 50.8% drawdown, 19 wrong-direction bets) and **always-flat = 0**. The edge is selectivity + shorting the down-movers, not a rising market. **Robust to costs**: still +129% at 50 bps round-trip (`--sensitivity`). So the same LLM that *fails* the overnight accuracy test (0.55 vs 0.69) *wins* the money test - because accuracy is the wrong scorecard.

**Honesty caveats (in the shareable report and to be said aloud to the group):** small sample (44 trades over 5 years; a few large Netflix/Boeing gaps do much of the lifting - concentration risk); costs modelled as bps but slippage/borrow/fills not separately simulated; **not tuned to P&L** (uses the fixed deployed 0.8/0.2 config - an honest read, not a curve fit); overnight is the hard window (the signal wins accuracy on the 5-day horizon, wins money on overnight); directional evidence of tradeable edge, not a validated live strategy.

**Shareable deliverable:** a self-contained equity-curve report (Artifact) with the curve, direction-error breakdown, cost-sensitivity, and caveats, for presenting to the group.

**Not done / open:** (a) **humans are not yet scored** - only the LLM's 131 decisions live in the repo; the human comparison activates the moment a group-sheet CSV is passed to `--sheet` (nothing about human decisions is in the codebase yet). (b) A **common-quarters mode** (score all raters only on the prints every rater covered) is not built - currently each rater is scored on their own rated quarters, so cross-rater *totals* are not strictly comparable (per-quarter/shared-print reads are). (c) The 107-quarter news backfill remains open (see above - now with a quantified reason it is not the overnight lever). `b3_window_sweep.py` / `overnight_pnl.py` are throwaway diagnostics kept in the session scratchpad, not committed (like earlier rounds' one-shot tools); `backtest.py` is the committed successor.
