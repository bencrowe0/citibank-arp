# CLAUDE.md

**ANCHOR CORRECTION (2026-08-12):** `release_date` (from EDGAR 8-K Item 2.02) is the sole entry anchor. `report_date` is a historical record only — 50 of 235 US events have `report_date ≠ release_date`. The exclusion set is 39 events (25 worksheet contamination + 1 SPOT misattribution + 13 timing unresolved), giving **N=229**. The pre-registered grading band is ±2% raw overnight. All figures below pre-dating 2026-08-12 are **SUPERSEDED** — they were computed on the old `report_date` anchor, which used post-announcement close as entry for 82 pre_market events. See `outputs/global/summary/retracted_findings_2026-08-12.md`. Corrected headline: **142/229 trades, 92 graded, accuracy 65.2% (60/92), mean net per trade +1.877%**.

Citibank Applied Research Project - earnings materials in, DeepSeek-powered structured BUY/HOLD/SELL predictions out, calibrated against actual stock price outcomes.

**Phase2 (`p2_*` issuers) is the only active track.** An earlier 6-issuer pipeline (`jpm`, `bank_of_america`, `boeing`, `disney`, `target`, `netflix`) was used to build and validate this pipeline before phase2 existed; its docs/manifests/outputs are still on disk but are retired - not run by default, not used for comparison, and shouldn't be cited going forward. Every bare CLI invocation below now defaults to the phase2 issuer set; pass an explicit issuer name to touch a retired one.

## Architecture

Four scored layers, blended into one signal, run per-issuer (73 issuers currently registered under `PHASE2_ISSUERS` in `blend.py`/`quant_layer.py`/`llm_news.py`, all `p2_`-prefixed; 71 are actively scored as of 2026-08-09 - `colgate_palmolive` and `costco` have manifests and sourced docs but await micro-layer scoring, see Known gaps):

- **Micro** (`report_pipeline.py`, `run_reports.py`) - the company's own earnings materials (press release + presentation + transcript, bundled into one LLM call per quarter). Core layer; everything else adjusts it. Scoring anchors to **change in forward guidance vs. the company's own prior guidance**, not absolute quarter quality - a "solid, modest beat, stable guidance" quarter scores ~0.0, not a default +0.5.
- **Macro** (`llm_macro.py`) - FOMC minutes, scored once per meeting, reused across every company/quarter whose `report_date` falls after that meeting (cached under `outputs/macro/results/`).
- **News** (`llm_news.py`) - a **pre-earnings market-expectations digest** per company/quarter, sourced from free outlets (CNBC, Yahoo Finance), stored under `docs/news/<issuer>/<document_id>.txt` with source URLs + dates inline. **Every source article is dated strictly before `report_date`** (hard rule - an earlier version leaked post-earnings reaction language; a second, distinct failure mode - digest *files* quarter-shifted onto the wrong `document_id` - was found and fixed 2026-08-09, see Known bugs fixed). Scores how stretched/depressed market expectations were heading into the print, not sentiment about results. Not every quarter has a digest yet - see Known gaps. **News currently earns 0.0 blend weight** (see `DEFAULT_WEIGHTS` above), so news-layer fixes/gaps do not move today's backtest numbers - they matter for any future weighting that reintroduces news, and for `experiments/rq16_surprise_control.py`-style analyses.
- **Quant** (`quant_layer.py`) - deterministic, yfinance-only, zero-LLM-cost composite of price momentum, EPS surprise, and a macro-numeric sub-component (`^TNX`, `^IRX`, `^VIX`), plus optional FRED Fed-data ablation (`DFF`, `T10Y2Y`, key-free `fredgraph.csv`, cached to `data/quantitative/fred_cache/`). Never earns blend weight (below).
- **Blend** (`blend.py`) - weighted sum of the four layer scores, then `hold_upper`/`hold_lower` thresholds derive BUY/HOLD/SELL. **As of 2026-08-05, `DEFAULT_WEIGHTS = (0.55, 0.45, 0.0, 0.0)`** (micro/macro/news/quant), thresholds `hold_upper=0.25` / `hold_lower=-0.05` (asymmetric; canonical constants `DEFAULT_HOLD_UPPER`/`DEFAULT_HOLD_LOWER` now live in `blend.py` itself and `eval/calibrate.py` imports them from there - `blend_document()`/`blend_issuer()`'s own defaults used to silently diverge to `±0.15`, fixed 2026-08-09, see Known bugs fixed).
  - **This was promoted by explicit user decision, overriding the project's own validity gates.** It's the exact combo the phase2 P&L sweep (`experiments/phase2_pnl_weight_threshold_sweep.py`) surfaced as its in-sample "best" at N=161 (167.66% total return, 99 trades) - but that same sweep run measured **PSR (Deflated Sharpe) = 0.0** and **permutation p = 0.150**, i.e. it failed the exact statistical checks this project built to catch overfit combos (see Current State). The user was shown this and chose to promote it anyway (2026-08-05) after also asking about a more conservative `0.4/0.4/0.1/0.1` split, which was not pursued. Treat the new default as **a deliberate, risk-accepted choice, not a newly-validated finding** - don't cite the PSR/permutation failure as "since fixed" in any future session.
  - Prior deployed default (0.8/0.0/0.2/0.0, `±0.25`) is now superseded; historical bullets below that reference it are dated and kept for the paper trail, not as current numbers.
  - **Derived artifacts were regenerated 2026-08-09** (post audit-fix pass, see Current State) - `outputs/global/summary/global_outcome_calibration_phase2.csv` and `backtest_equity.csv` reflect the current default weights/thresholds AND the audit fixes (corrected report_dates, re-sourced news digests, colgate_palmolive/costco manifests). Re-run both commands again before trusting these numbers if you've since scored new combos or touched `report_dates.json`/any manifest.
  - **Regenerated again 2026-08-10** after fixing `AMKBY_FQ4_2025`'s corrupted `report_date` — **SUPERSEDED 2026-08-12** by the anchor correction. The figures below (171/268 trades, +1243.64% total return, 62.6% hit rate, Sharpe/trade 3.58, 37.40% max drawdown, Correct/Flat/Wrong 60/84/27) were computed on the old `report_date` anchor and the old N=268 universe. They are retained as a historical record. The compounded total return (+1243.64%) is order-dependent and not an achievable balance because positions overlap across same-week reporters — mean net per trade with a bootstrap interval is the correct headline. The figure labelled "Sharpe/trade 3.58" is `mean(nets)/pstdev(nets)*sqrt(N)` — a t-statistic (per-trade information ratio scaled by sqrt(trade count)), not a time-series Sharpe ratio. `backtest.py` now returns this as `t_statistic` (renamed from `sharpe_per_trade`); the unscaled `info_ratio_per_trade` is also available.

Micro/macro/news share one prompt (`prompts/llm_analysis_prompt_template.md`), differing only in `{{REPORT_TYPE}}`/`{{REPORT_TEXT}}`. Quant is pure arithmetic, no LLM call.

## Manifest schema

`manifests/p2_<slug>_reports.json` - each report entry has a `documents` list: `[{"doc_type": "Press Release", "source_pdf": "..."}, ...]`. Composition varies by issuer. `report_pipeline.build_bundle_text()` concatenates a report's documents into one labeled text block per quarter. Every phase2 manifest entry has ≥2 documents. `p2_jpm`'s `JPM_2026_Q1` was previously skipped (no usable source document existed) - closed once free transcripts (Motley Fool, Benzinga, The Globe and Mail) became available after the 2026-04-14 call; see Known bugs fixed.

Long filings (10-K/10-Q, annual reports) are excluded by filename pattern (`phase2/build_manifests.py`) - too dense, matches the group's "too financy" feedback.

## Dataset naming convention

Every scored source document lives under **`docs/<issuer>/phase2/CY<FY>-Q<FQ>/`** (the 6 issuers that also have a retired pre-phase2 folder) or **`docs/<issuer>/CY<FY>-Q<FQ>/`** (the 34 phase2-only issuers, no collision risk), where `CY<FY>-Q<FQ>` is the **fiscal period being reported** (from manifest `fiscal_period`, e.g. `FQ1 2026` -> `CY2026-Q1`), NOT the calendar quarter of the release date. Filenames stay issuer-native; only the folder is normalized. Result artifacts: `outputs/p2_<slug>/results/<TICKER>_FQ<Q>_<YEAR>.json`. Any future backfill must follow this scheme.

## Running the pipeline

See [README.md](README.md) for micro-layer run commands. Macro/news/blend/eval/backtest - bare invocations default to the full phase2 issuer set; pass explicit issuer names to touch a retired pre-phase2 issuer:

```powershell
python llm_macro.py                         # score all FOMC minutes (cached, idempotent)
python llm_news.py                          # score all phase2 pre-fetched news digests (cached, idempotent)
python quant_layer.py                       # score all phase2 quarters' quant composite (free, cached, idempotent)
python blend.py <issuer>                    # blend micro+macro+news+quant for one issuer's quarters
python -m eval.run_eval                     # calibrate threshold + blend weights, pooled across every phase2 issuer (same as --phase2-all)
python -m eval.run_eval --output-suffix phase2   # writes global_outcome_calibration_phase2.csv (the canonical phase2 output)
python -m experiments.phase2_pnl_weight_threshold_sweep   # phase2-scoped weight/threshold grid, TOTAL-RETURN objective + LOOCV + DSR
python -m experiments.rq16_surprise_control   # does the blend score add value beyond public EPS surprise%; also computes legacy production/pooled numbers against retired data (ignore those) - only outputs/global/summary/rq16_surprise_control_phase2.json matters
python build_cost_ledger.py                 # rebuild outputs/global/summary/api_cost_ledger.csv from existing phase2 artifacts (no API calls)
python backtest.py --calibration-csv outputs/global/summary/global_outcome_calibration_phase2.csv   # P&L backtest of the LLM's overnight-gap trades, net of costs
python backtest.py --sheet export.csv       # score every HUMAN rater from a group-sheet CSV export on the identical strategy
```

`run_reports.py` also takes `--prompt <file>` (test an alternate prompt without touching the default), `--ensemble N` (average N repeat runs per document), and `--variant <label>` (writes to `outputs/<issuer>_<label>/`, never overwrites canonical). `eval.run_eval --issuers <issuer>_<label>,...` evaluates a variant folder. `compare_runs.py <a> <b>` diffs two results folders, optionally against ground truth.

## Two distinct threshold concepts - do not conflate

- `hold_upper` / `hold_lower` - thresholds on the LLM's **sentiment score** (-1..+1), used to derive a prediction.
- `outcome_upper` / `outcome_lower` (`eval/outcomes.py`) - thresholds on the stock's **actual forward return**, used to derive the ground-truth label a prediction is checked against.

Never reuse the `hold_*` names for the outcome-side threshold or vice versa.

## Two distinct performance metrics - do not conflate (this bit us: a memory check answered "5-day" for phase2 when the real answer is overnight)

- **Accuracy/calibration numbers** (`eval/run_eval.py`, `global_outcome_calibration_phase2.csv`) use `eval/outcomes.py`'s `fetch_forward_return(window_trading_days=5, exit_on_open=False)` by default - **5-day close-to-close**. This is a calibration/threshold-tuning artifact (accuracy, LOOCV, weight sweeps), not the deliverable metric.
- **P&L/backtest numbers** (`backtest.py`'s `overnight_gap()`/`simulate()`, the group sheet, every "+X% total return" figure quoted anywhere in this doc) compute close→next-open directly from `prior_close`/`next_day_open` (or `fetch_prices`), **always overnight, regardless of what `run_eval` used** - `simulate()` never reads `window_trading_days` or the calibration CSV's `forward_return` column.
- **Rule of thumb: if the number is a total return / Sharpe / trade count / P&L figure, it's overnight. If it's a bare "accuracy" or "balanced accuracy" percentage, it's 5-day (or whatever `--window-trading-days` was passed to that specific `run_eval`/sweep call).** Before answering "is X's performance measure overnight or 5-day," grep the actual code path (`backtest.py` vs `eval/outcomes.py`) rather than pattern-matching this doc's prose.

## Current state (phase2, N=229 after exclusions)

**SUPERSEDED 2026-08-12.** All figures in this section pre-dating 2026-08-12 were computed on the old `report_date` anchor and the old N=268 universe. They are retained below as a historical record with `[SUPERSEDED]` labels. The corrected state after the anchor correction and exclusion audit:

- **Corrected (2026-08-12), release_date anchor, N=229**: 142/229 trades, **92 graded directional events, accuracy 65.2% (60/92), mean net per trade +1.877%**. The pre-registered grading band is ±2% raw overnight. 39 events excluded (25 worksheet contamination, 1 SPOT misattribution, 13 timing unresolved). See `outputs/global/summary/effective_sample_funnel.md` for the full funnel. The compounded total return figure is no longer quoted as a headline — it is order-dependent and not an achievable balance. Mean net per trade with a bootstrap interval is the primary P&L metric.

- **[SUPERSEDED, pre-anchor] N=268 rerun (2026-08-10)**: 171/268 trades, total return +1243.64%, hit rate 62.6%, Sharpe/trade 3.58, avg/trade 1.719%, Correct/Flat/Wrong 60/84/27. These figures used `report_date` close uniformly as entry, which was wrong for 82 pre_market events. The figure labelled "Sharpe/trade 3.58" is a t-statistic (`mean(nets)/pstdev(nets)*sqrt(N)`), not a time-series Sharpe ratio (`backtest.py` now returns this as `t_statistic`). The +1243.64% is a compounded equity curve, order-dependent.
- **[Historical, N=161] Backtest under the OLD default (`0.8/0.0/0.2/0.0`, `±0.25`)**: 79/161 trades, total return +64.51%, hit rate 58.2%, Sharpe/trade 1.29, max drawdown 39.52%, Correct/Flat/Wrong = 25/37/17. Kept for the paper trail only - this is no longer the deployed weighting, see Architecture > Blend.
- **[Historical, N=161] Current-default performance at the time of promotion** - same combo the P&L sweep scored: total return 167.66% (tensor sweep) / 167.96% (self-check via `backtest.simulate()`), 99 trades, hit rate 61.6%, Sharpe 2.329-2.332, avg/trade 1.11%. **PSR=0.0, permutation p=0.150 - failed validity, promoted anyway by explicit user decision** (see Architecture > Blend for full context) - this validity failure has not been rechecked at N=268; treat the N=268 numbers above as descriptive backtest performance, not a re-validated finding.
- **[Historical, under the OLD default weights, N=161] Threshold sweep** (`experiments/phase2_threshold_sweep.py`, `outputs/global/summary/phase2_threshold_sweep.tsv`) - the N=153 claim that tightening always hurts did NOT hold at N=161: `hu=0.15` (123 trades) beat the deployed `hu=0.25` on total return (84.29% vs 64.51%). The single-metric total-return-optimal point on that grid was `hu=0.35` (47 trades, total return 106.91%). A single in-sample grid with no LOOCV/DSR/permutation check - directional signal only, not validated, and not rechecked at N=268.
- **The blend score does not clearly beat the public EPS surprise%** (`experiments/rq16_surprise_control.py`) - last run at N=158 (161 minus 3 missing surprise data): surprise-only accuracy 0.437, LLM-only 0.386, majority baseline 0.468 - **the baseline still beats both individual predictors**, R² lift from adding the score not significant (p=0.456). An honest, currently-unresolved limitation - not yet rerun at N=268.
- **[Historical, N=161] The weight/threshold sweep flagged this exact combo as an overfit, at the time** - `experiments/phase2_pnl_weight_threshold_sweep.py` (113,344-combo grid): in-sample "best" combo showed total return 167.66%, raw Sharpe 2.329, but **PSR=0.0, permutation p=0.150**. LOOCV total return (87.82%, 95 trades) was positive and beat the then-deployed default's 64.3%, but PSR/permutation still said the in-sample edge was trial-count luck. **This is exactly the combo promoted to `DEFAULT_WEIGHTS` on 2026-08-05 anyway** (see Architecture > Blend). Not rechecked at N=268.
- **Quant earns zero blend weight**; macro went from 0.0 to 0.45 in the 2026-08-05 promotion above - no longer zero.
- **`colgate_palmolive` and `costco` are not yet in the N=268 count** - manifests exist (4 and 3 quarters respectively, built 2026-08-09) but the micro layer hasn't been scored (costs API calls, deferred pending a decision on whether to spend it). Scoring them would raise N to 275 at most.

## Known gaps / not yet done

- **Settle on a properly-validated weighting, not the current risk-accepted default.** The 2026-08-05 promotion (`0.55/0.45/0.0/0.0`, thr `+0.25/-0.05`, see Architecture > Blend) deploys a combo with PSR=0.0 and permutation p=0.150 - i.e. picked for backtest performance, not because it passed the project's own overfitting checks. That validity check has not been rerun at N=268. Future work: search for (or confirm none exists at this N) a weighting that backtests well AND clears PSR/permutation/LOOCV - e.g. constraining the search to fewer free dimensions (fixed quant=0, coarser weight step, symmetric threshold only) to lower the trial count DSR corrects for, rather than the full 113k-combo grid that guarantees an inflated-looking winner.
- **`colgate_palmolive` and `costco` have manifests but no micro-layer score yet** (built 2026-08-09 from already-sourced docs - 4 and 3 quarters respectively; `hermes` was dropped from `PHASE2_ISSUERS` entirely, it never had any docs). Scoring costs API calls, deferred pending a decision - would raise N from 268 to at most 275.
- **No news digest**: Maersk Q1/Q4 2025 (thin free US-financial-media coverage for a Danish-listed shipper) and Netflix Q4 2024 (deprioritized for time). `BA_FQ4_2025` and `KHC_FQ1_2026` (previously undocumented gaps, found by the 2026-08-09 audit) are now closed - see Known bugs fixed.
- **`GS_FQ2_2026`** - reported 2026-07-14; was too recent for a resolved forward-return outcome as of the last run. May be resolvable now given today's date - not yet rechecked.
- **Report-date reconciliation (2026-08-09 audit)**: of the 33 phase2 combos whose human-typed price pair doesn't cross-validate against real trading days, 4 turned out to be genuine `report_date` bugs and were fixed (`AMKBY_FQ1_2025` was assigned the wrong quarter's date entirely; `DIS_FQ1_2025` had its fiscal-Q2 date instead of Q1; `PUM.DE_FQ3_2025` was off by 1 day; `LNVGY_FQ2_2026` had a year typo). The remaining 29 were individually cross-checked against real-world earnings calendars and confirmed correct - the mismatch is attributed to human price-entry typos, not date errors (annotated in `phase2/report_dates.json`). This supersedes the old "22 unresolvable" figure, which predated this audit and wasn't reconciled against the mechanical checker's actual count. 5 further combos remain genuinely ambiguous (multiple candidate trading days) - unreviewed: `BAC_2025_Q2`, `F_2025_Q2`, `PUM.DE_2026_Q1`, `V_2025_Q3`, plus `LNVGY_2026_Q2` which is resolved via reasoning (see Known bugs fixed) but whose ambiguity-checker window doesn't reach far enough to price-validate the corrected date.
- **4 pre-existing thin JPM 2025 quarters + 1 PepsiCo quarter** with no free transcript found - surfaced but out of scope, not fixed.
- **6 cost-ledger rows (Hilton x2, JPM x4) permanently unrecoverable, not just stale** - `build_cost_ledger.py` was rerun 2026-08-09 (see Known bugs fixed) but these 6 scored results predate `cost_log` tracking entirely (no cost data was ever recorded at scoring time) - rerunning the ledger builder cannot recover them; only a paid rescore could, and hasn't been done.
- **Live sheet open item**: confirm `Settings!B3` reads 2.0 (not 10.0, from a caught-but-unverified misclick); the guarded `Data Entry!P` formula needs typing once and copying down the full range; confirm `B4`/`B5` populated (10/0). *(This item predates the `Master_Data_NEW.ods` schema migration - recheck against the new `Settings` tab, which as of 2026-08-05 does read 2.0/10/0 correctly.)*
- **`Master_Data_NEW.ods`'s `Company_List` tab doesn't cover phase2** - see Live sheet section. Needs the 40-ticker phase2 roster added (or the `Ticker` auto-lookup will silently blank for every non-legacy company).

## Model-arm robustness spec (Nigel, 2026-08-05) - Tier 1 + Tier 2 deliverables (2026-08-10)

Nigel (MSc teammate) sent a full 8-item spec for the model arm's remaining pre-deadline work (deadline 1 Sept 2026), covering robustness/validity checks an examiner would probe. The spec file itself lives outside this repo, at `C:\Users\bencr\Downloads\WhatsApp Chat - Nigel (Bayes)\00000701-Model_Arm_Implementation_Spec.md` (from the WhatsApp chat export, **not committed** - same "lives outside the repo, be careful" caveat as `Master_Data_NEW.ods`). Its own file/number references were stale against this repo's actual state at the time (it said "120 events" - actually N=268; `Master_Data_NEW.xlsx` - actually `.ods`; `p2_[company]/summary/*.csv` - actually `outputs/p2_<slug>/results/*.json` + `outputs/global/summary/*.csv`) - confirmed with the user that N=268 and this repo's real layout are what matter, not the spec's literal paths.

The 8 items were triaged for ROI given a tight token/time budget. **Tier 1** (5 cheapest/highest-defensive-value items) was built with **zero new LLM API calls** (pure recomputation over already-scored data). **Tier 2** (this section's second half) adds the contamination audit, execution-cost grid, walk-forward freeze note, and the Loughran-McDonald baseline - one real API call cost (~31 calls, contamination audit). The remaining expensive deferred items (full section ablation, FinBERT, full walk-forward validation, the human-line halves of Extensions 2/4/9/1) are **not started** - see Known gaps.

### Tier 1

**New files** (all read-only against existing scored data, no rescoring):
- `bootstrap_stats.py` (repo root, shared infra) - `bootstrap_trade_stats` (one shared resample stream across total return/mean-per-trade/hit-rate/Sharpe so they stay mutually consistent), `bootstrap_paired_difference` (same-event pairs, e.g. macro-on vs macro-off), `bootstrap_unpaired_difference` (different-event groups, e.g. BUY-truth vs SELL-truth), `bootstrap_clustered_by_week` (resamples ISO-week clusters of report dates, not individual events - optional, unused by Tier 1 beyond an optional CI). `RNG_SEED = 20260709` matches the project's existing sweep-script convention.
- `experiments/leave_one_out_robustness.py` (Extension 5) - ticker/calendar-quarter/single-event leave-one-out on `backtest.simulate()` against `global_outcome_calibration_phase2.csv`. Uses the commutative-product shortcut (`eq_full / (1+net_i) - 1`) for a full-sample fast pass, real resimulate only for the top 10 by impact.
- `experiments/quote_verification_screen.py` (Item 8) - verifies `evidence[].quote` against each event's cached `run_meta.extracted_text_path` source text. Exact substring, then `rapidfuzz.fuzz.partial_ratio` fallback at 90%. Ellipsis-aware: a quote containing `...`/`…` is split into segments and each verified independently, since the model uses ellipsis as an explicit elision marker on table rows/long transcript answers.
- `experiments/asymmetry_conviction_analysis.py` (Item 6) - recomputes the **deployed blended score** (not micro alone - locked decision) per event via `blend.blend_scores(..., DEFAULT_WEIGHTS)`, then direction confusion matrix, HOLD-rate-by-truth, hit-rate-by-move-magnitude bins, score-magnitude-vs-accuracy bins + `conviction_curve.png`, Spearman rank correlation, and a BUY-vs-SELL recall-gap test (primary: closed-form two-proportion z-test via scipy; companion: `bootstrap_unpaired_difference` CI - the comparison is unpaired, different events per group, so the paired bootstrap variant doesn't apply here).
- `experiments/macro_weight_axis_sweep.py` (Item 1a) - a **focused re-run**, deliberately NOT reusing `experiments/weight_threshold_sweep.py`'s `load_pooled()` (that draws from `ALL_ISSUERS`, a pooled original+phase2 universe of N=232, not the current N=268 phase2 track - reusing it would have silently scored the wrong dataset). Sweeps macro weight 0.0-1.0 in 0.05 steps directly against the phase2 calibration CSV, HOLD thresholds held fixed at the deployed default (locked decision - isolates the weight effect from a threshold re-search) - produces `macro_weight_curve.png`.
- `experiments/macro_ablation_analysis.py` (Item 1b/1c) - paired per-event macro-on (`DEFAULT_WEIGHTS`) vs macro-off (`(0.55,0,0,0)`, which renormalizes to `blended == micro_score` exactly - asserted in the script) accuracy, `bootstrap_paired_difference` CI, and a sector split via `phase2/build_manifests.SECTORS`. Deliberately does NOT use the calibration CSV's own `blend_correct_default`/`blend_correct_tuned` columns for this - `blend_tuned_weights` is a constant `(0.6, 0.0, 0.4, 0.0)` across every row, which moves micro and news simultaneously with macro, not a clean ceteris-paribus ablation.
- Item 1d (point-in-time discipline paragraph) is prose only, no new code - cites `llm_macro.py`'s `get_macro_score_for_date()` (already filters FOMC meetings to `minutes_release_date <= report_date`, takes the latest eligible one).
- `requirements.txt` - added `rapidfuzz`, `matplotlib` (new), and declared `numpy`/`scipy` (pre-existing undeclared dependencies already used throughout `experiments/`, not introduced by this work).

**Results** (all outputs under `outputs/global/summary/`, all cross-verified against each other and against known figures elsewhere in this doc):

- **Leave-one-out**: full-sample row reproduces this doc's own N=268 backtest figure exactly (171 trades, +1265.55% total return, 62.6% hit rate) - confirms the wrapper didn't diverge from `backtest.simulate()`. Total return sign never flips under any single ticker/quarter/event exclusion; biggest single-ticker impact is dropping CMG (-353pp, still +912% remaining).
- **Quote screen**: 272 evidence items sampled across 15 documents (stratified by sector x outcome_label). 267 passed to `quote_verification_handover.csv`. Of the 5 genuine rejects: 4 are `Comcast` table quotes that prepend a row-group header not contiguous with the data row in the linearized source (real content, non-verbatim reconstruction - a minor, reportable prompt-compliance gap, not fabrication) and 1 is a **real fabrication** - `PUM.DE_FQ1_2025`'s evidence claimed "North America -11.1%" but `-11.1%` in the source actually belongs to a non-controlling-interests line, not the North America region. Before the ellipsis-segment fix, the naive whole-string matcher false-rejected 9 additional genuine (elided, not fabricated) quotes as absent - fixed during this build, not a pre-existing pipeline bug.
- **Asymmetry/conviction**: recall gap BUY-truth 36.1% (n=72) vs SELL-truth 31.6% (n=76), gap +4.5pp, **not significant** (z-test p=0.56, bootstrap CI [-7.8%, +17.0%]) - contradicts the spec's own prediction of a real asymmetry; report as the null it is. Score magnitude doesn't predict move size either (Spearman rho=0.108, p=0.079, n=268).
- **Macro write-up**: curve confirms the "near-zero is better" direction - macro=0.0 accuracy 0.3731 beats deployed macro=0.45's 0.3619 (best point on the curve is macro=0.35 at 0.3881). Paired bootstrap on the on/off accuracy difference: -1.12pp, CI [-5.60%, +3.36%], **not significant** (p=0.739) - a real, honest null, not the confident-sounding "0.466 to 0.443" figure quoted elsewhere in this doc's Architecture/Current-state sections (that figure could not be reproduced from any stored artifact during this work - `weight_threshold_sweep.json` only ever persisted the single winning combo, never a weight-vs-accuracy curve - treat "0.443" as unverifiable going forward, this new curve is the citable replacement). Sector split (38 fine-grained, non-GICS-standardized sector buckets from `SECTORS`) is noisy as expected given small per-sector n - report the null plainly rather than over-reading any single sector's swing.
- **Deployed-default accuracy figure**: 36.2% (5-day window, N=268, `DEFAULT_WEIGHTS`, default thresholds) - verified with zero mismatches against the calibration CSV's own `blend_predicted_signal_default` column. Distinct from the 0.4656 `global_best` figure in `weight_threshold_sweep.json` (different weights `(0.8,0,0.2,0)`, LOOCV-tuned thresholds, and a different/older dataset) - **do not conflate the two accuracy figures**, they answer different questions.

### Tier 2

**New files:**
- `experiments/execution_cost_grid.py` (Extension 9) - 12-cell grid, round-trip cost {10,20,30,50}bps x entry slippage {0,10,20}bps. `backtest.simulate()` has no separate slippage parameter, so slippage is modelled as additive to `cost_bps` (`total_cost_bps = round_trip + slippage`), stated explicitly rather than silently. Also bisects the actual breakeven `cost_bps` on the live N=268 backtest, since the spec's own "~113bps, already computed" claim could not be verified anywhere in this repo (grepped code, docs, outputs - nothing).
- `outputs/global/summary/ext1_freeze_note.md` (Extension 1's freeze note, prose only) - dated 2026-08-10, freezes the deployed weights/thresholds/cost assumption/horizon, explicitly labelled retrospective not pre-registered (all 268 events already seen). The full walk-forward build itself stays out of scope.
- `experiments/lm_baseline.py` (Item 4, word-list half only - FinBERT half deferred, new heavy torch/transformers dependency) - Loughran-McDonald word-count score via `pysentiment2` (bundles the canonical LM Master Dictionary, added to `requirements.txt`, no manual download). New dev/eval split (didn't exist before): earliest 20% of N=268 by `report_date` = dev (thresholds fitted there only), remaining ~80% = eval (frozen thresholds applied, never fit on the events being scored).
- `experiments/contamination_audit.py` (Item 5) - asks the model its own training cutoff directly (1 call) rather than assuming one, splits N=268 pre/post that date, bootstraps the accuracy difference (`bootstrap_unpaired_difference` - different events per group, same reasoning as the Tier 1 recall-gap test), then a ~30-event recall probe (no document attached, verbatim logged, classified correct-direction/wrong-direction/refused). Uses the raw `OpenAI`/DeepSeek client directly, not `report_pipeline.call_llm()` (that's schema-coupled to the `{sentiment,signal,summary,evidence}` pipeline output and would reject a free-text answer).
- `experiments/extension4_conviction_sizing.py` (Extension 4, **model side only** - human-line half still waits on Nigel's team's reading sessions) - recomputes the deployed blended score per event (same locked `blend.blend_scores(..., DEFAULT_WEIGHTS)` pattern as Item 6, cross-checked row-by-row against the calibration CSV's `blend_predicted_signal_default`). Two books over the identical N=268 event set: **flat** is `backtest.simulate()` completely unmodified (doubles as a sanity check - see Results); **sized** replicates `simulate()`'s net-return math with a per-trade multiplier `size_i = |score_i| / mean(|score|)`, mean taken over the 171 *traded* events only (not all 268 - a stated modeling choice, since sizing has no meaning for a HOLD). Dividing by that mean is what keeps the sized book scale-invariant (mean size exactly 1.0x) - without it the comparison is just a bigger book winning by construction, the spec's own named trap. Cost scales with size in lockstep with P&L (`net_sized = size * net_flat`) since `cost_bps` is a percentage-of-notional fee. Normalizing by a full-sample statistic (mean \|score\| with hindsight over all 268 events) is itself mild look-ahead - small at this N, stated rather than glossed over, same as every other look-ahead risk this project has flagged. Outputs `outputs/global/summary/ext4_conviction_sizing.csv` and `ext4_score_distribution.png`.

**Results:**
- **Cost grid**: cost_bps=10/slippage=0 cell reproduces the live backtest total return exactly (sanity check passed); total return decreases monotonically across all 12 cells and stays positive throughout (+386.75% even at the worst cell, 50bps round-trip + 20bps slippage = 70bps total). Bisected breakeven: **162.81bps** - this repo's own real number, genuinely different from the spec's unverifiable "~113bps" claim (expected, since that figure was computed on a different N/weight combination that doesn't exist in this codebase).
- **LM baseline**: on the eval split (n=214, same events scored both ways), LM baseline accuracy 0.3785 vs deployed model 0.3972 vs majority-class(HOLD) baseline 0.4252 - the model narrowly beats the cheap baseline, but **the majority-class baseline still beats both**, same honest-limitation shape as the existing `rq16_surprise_control` finding (don't smooth this into a model-favorable framing).
- **Contamination audit**: model self-reported training cutoff = **October 2023** (verbatim, taken at face value per the spec's own "ask directly" methodology, not independently verified - LLM self-reported cutoffs are known to be imprecise). Since this project deliberately scores 2024-2026 earnings, only **2 of 268 events predate that cutoff** - the pre/post split is statistically degenerate (n=2 vs n=266) and its accuracy-diff/bootstrap-CI/p-value are reported for completeness only, not as a finding. The recall probe is the actually informative result: **30/30 sampled events (100%) got a refusal** - the model consistently said it had no specific knowledge rather than confabulating a direction, for every ticker/quarter sampled, old or recent. This **contradicts the spec's own prediction** (it expected a mix - confident recall on old quarters, refusals on new ones - as "the clearest single piece of evidence available in either direction"); report the null plainly. It's also a structurally reassuring contamination signal for a different reason than the spec anticipated: with the scored dataset almost entirely postdating the model's own stated knowledge, there's little for it to have memorized in the first place.
- **Incidental finding while building the LM baseline**: `AMKBY_FQ4_2025`'s `report_date` was corrupted (`2014-02-28`) - see Known bugs fixed. Fixed inline (user approved), `eval.run_eval`/`backtest.py` rerun, headline total return moved +1265.55% -> +1243.64% (Current State has the updated figures).
- **Conviction sizing (Extension 4, model side)**: flat book reproduces the doc's own N=268 headline exactly (171 trades, +1243.64% total return, 62.6% hit rate, sharpe 3.581, max_dd 37.40%) - sanity check passed. Sized book: 171 trades (same set, hit rate unchanged at 62.6% since sizing doesn't touch direction), total return **+1272.45%**, but **sharpe 3.036** (down from 3.581) and **max_dd 41.44%** (up from 37.40%) - a worse risk-adjusted book, not a clean win. Paired bootstrap on mean-net-per-trade (sized - flat, same 171 trades): **+0.10pp, CI [-0.38pp, +0.61pp], not significant (p=0.773)**. Total-return bootstrap CIs overlap heavily (flat +1244%, CI [+273%,+4743%]; sized +1272%, CI [+201%,+6514%]). **A null, consistent with Item 6's own finding** that score magnitude doesn't predict move size (Spearman rho=0.108, p=0.079) - conviction sizing doesn't clearly help here, report as the null it is rather than reading the nominal total-return edge as a win. \|score\| distribution going in: mean=0.227, median=0.230, stdev=0.141, range [0.0, 0.80], IQR [0.12, 0.305] - reasonably well-spread (not bunched near zero or near the HOLD-band edges), so this null describes a real test of the sizing idea, not an artifact of a flat score distribution.

**Not yet done (beyond Tier 2)** - see the spec file (path above) for full detail on each:
- Full section ablation (Item 3, deferred - most expensive item, ~750+ new API calls + manual doc-splitting)
- FinBERT half of Item 4 (deferred - new heavy torch/transformers dependency)
- Extension 1 full walk-forward validation (deferred - most expensive code item, 4-6hrs even trimmed to the two-window headline; the dated freeze note above stands on its own regardless)
- Extension 4's own human-line half (built above, model side only) and Extensions 2/9's human-line halves, and Extension 1's human-line, all wait on Nigel's team's reading sessions regardless of build order.

## Live "Human vs LLM" group sheet

**Schema changed mid-project - two generations exist, do not conflate them:**

1. **Retired schema** (`Human vs LLM results.xlsx`/`.ods`, Nigel's SharePoint, single combined `Data Entry` tab) - documented in git history if needed, superseded by (2). `phase2/export_rows.py` / `phase2/data_entry_rows_aggressive.tsv` target this old schema.
2. **Current schema** (`Master_Data_NEW.ods`, local copy at repo root, **not committed** - same underlying SharePoint workbook, restructured) - split into separate `Human_Data_Entry` and `LLM_Data_Entry` tabs, plus `Settings`, `Summary`, `Company_List`, `Efficiency` (new), `Charts`. This is the one to target for any new export. `Master_Data_NEW.ods` is malformed OOXML (odfpy/pandas fail with `duplicate attribute` / `ExpatError`) - read it with `lxml.etree.fromstring(data, parser=XMLParser(recover=True, huge_tree=True))` on the raw `content.xml` from the zip, not pandas/odfpy directly.

Every rater (human or LLM) auto-scores on P&L, not just accuracy, as rows are typed. **Never drive this sheet live via computer-use/browser clicks** - two misclicks during earlier live editing overwrote a banner and a threshold cell; the user has explicitly ended Claude-driven live edits. All changes go to the user as a paste-ready TSV or formulas to type by hand. **Also never paste a formula copied from chat into the sheet** - Excel Online's clipboard link metadata intermittently rewrites cross-tab references into a broken external-URL self-reference; only hand-typing formulas (or pasting literal computed values, which is what our export scripts now do) is reliable. Internal same-workbook copy/paste (filling a formula down rows) is safe.

Current schema structure (must match exactly if extended; data rows start at row 3):

- **`LLM_Data_Entry`** - 21 columns, A-U, in order: `Company, Ticker, Year, Quarter, Rater, Type (Human/LLM), Sentiment Score (-1 to +1), Decision (BUY/HOLD/SELL), Time (seconds), Document Date, Closing Date, Prior Close ($), Opening Date, Next Day Open ($), Token Cost ($), Actual % Change, Actual Direction, Prediction Correct?, Position, Net PL, Notes / Risks Flagged`. New vs the retired schema: `Time` is now **seconds** (was minutes), and there are 3 new columns - `Document Date` (=`report_date`), `Closing Date`/`Opening Date` (the actual calendar dates behind Prior Close/Next Day Open - recover from `data/quantitative/sheet_export_cache/{TICKER}_{report_date}.csv`'s index, `export_sheet_rows.fetch_prices()` alone only returns the prices, not the dates), and `Token Cost ($)` (the micro-layer's `estimated_cost_usd` for that document from `outputs/global/summary/api_cost_ledger.csv`, `layer=="micro"` rows only - macro is excluded since one FOMC score is reused across many companies and isn't attributable to a single row). Was empty (header only) as of 2026-08-05.
- **`Human_Data_Entry`** - similar but keeps `Time (mins)` and has 4 extra hidden helper columns (`Time (sec)` computed from mins, an `Event Key` = `Company&"|"&Year&"|"&Quarter`, and two `XLOOKUP`s pulling the LLM's own Decision/Correct? from `LLM_Data_Entry` for that same event) so each human row can show the LLM's call side-by-side. **This means `Company` text and `Year`/`Quarter` must match exactly between the two tabs** for that per-row cross-reference to resolve - ticker casing/spelling drift breaks it silently (returns blank, not an error).
- **`Company_List`** - as of 2026-08-05 only lists the old 6 retired-track companies (Netflix, Target, Boeing, Bank of America, JPMorgan) plus a few generic extras (Apple, Microsoft, Alphabet) - **does not yet contain phase2's 40-ticker roster**. `Ticker` columns elsewhere `XLOOKUP` against this list; a phase2 company not on it resolves to blank. Not yet fixed/audited - flag for whoever owns the sheet, or extend it before relying on the Ticker auto-lookup for phase2 rows.
- **`Settings`**: same 3 cells as before - `B3` = HOLD/direction threshold (**2.0**), `B4` = round-trip cost bps (10), `B5` = short-borrow bps (0).
- **`Summary`**: Accuracy-by-Rater sits beside P&L-by-Rater (`Rater, Type, #Trades, Total Return %, Avg %/Trade, Dir. Hit Rate, Correct, Flat, Wrong`), same rater rows, plus an All-Humans aggregate row. **Avg Net P&L per Trade is the recommended primary judging metric** (fair across raters with different trade counts). Both blocks now read off `Human_Data_Entry`'s columns (see formulas), not the old combined tab.
- **`Efficiency`** (new tab) - return-per-unit-of-analyst-effort: `LLM cost per document` live-averages `LLM_Data_Entry`'s `Token Cost ($)` column (falls back to a manual placeholder while that column is empty), `LLM wall-clock per document` live-averages the `Time (seconds)` column likewise. **Keeping both columns populated on every LLM row matters** - this tab silently reverts to placeholder assumptions otherwise.
- **Extra rater row**: `Ben/DeepSeek (aggressive: micro.55/macro.45)`, generated by `phase2/export_rows_aggressive.py` -> `phase2/llm_data_entry_rows_aggressive.tsv` (99 trades, ~168% total return, matches the current `LLM_Data_Entry` column layout - self-checks against the sweep JSON). Its weights/thresholds are now identical to `blend.py`'s deployed `DEFAULT_WEIGHTS` (see Architecture > Blend) - it's no longer a distinct experimental combo, just a second row using the same weighting as the main `Ben/DeepSeek` row. It won't appear in the Summary tab's per-rater table automatically (those rater rows are static); add a row there manually if it needs to show up.

## Known bugs fixed (still worth knowing the failure mode)

- **Report-date resolution**: two compounding bugs in `phase2/resolve_report_dates.py` - (1) nearest-real-earnings-date positional alignment silently breaks whenever the human dataset lags the single most-recent real print, shifting every date forward a whole quarter; (2) matching against yfinance's *adjusted* (dividend/split-adjusted) price history silently fails since adjusted prices drift below what was actually quoted at the time. Fixed by `phase2/fix_report_dates_from_human_prices.py`, which cross-validates against the humans' own typed (Prior Close, Next Day Open) pair using raw (`auto_adjust=False`) price history - two independent price points must both match.
- **`phase2/export_rows.py` threshold trap**: was deriving `Decision` via `blend.blend_document()`, whose own function-default `hold_upper/lower` is `±0.15`, not the canonical `±0.25` used everywhere else (`eval/calibrate.py`, `backtest.py`, the deployed default). Fixed by reading `Decision`/`Sentiment Score` straight from the calibration CSV's `blend_predicted_signal_default` column.
- **`report_pipeline.extract_doc_text()`** only recognized `.html`, not `.htm` (the conventional SEC EDGAR exhibit extension) or `.txt` (used by several web-sourced transcripts) - silently failed extraction on both until widened.
- **`resolve_report_dates.py` fiscal-year `base_year` rule** mis-years any ticker whose Q1 starts in the first half of the calendar year but still belongs to the prior fiscal year (e.g. FedEx, FYE May 31) - fixed with an explicit `FYE_BASE_YEAR_OFFSET` override.
- **NFLX transcript mislabeling**: `NFLX_FQ1_2025`/`NFLX_FQ2_2025`'s transcript documents actually contained the FQ3/FQ4 2025 calls (future-information leakage into the micro layer). Re-sourced the correct transcripts; the real FQ4 2025 transcript found in the process was added to `NFLX_FQ4_2025` as a bonus doc.
- **News digest leakage**: an earlier version of the news-scoring window pulled from "earnings date and following days" and picked up actual post-earnings reaction language. Digests are now rebuilt strictly pre-`report_date`, with an explicit prompt rule to score pre-earnings expectations, not guidance change.
- **`backtest.py --sheet` parsing**: 3 bugs fixed - whitespace/case-insensitive header and rater matching, synthetic date fallback for non-calibration tickers. Committed (`1c420cf`).
- **`PHASE2_ISSUERS` drift**: `llm_news.py`'s copy of the list fell behind `blend.py`'s/`quant_layer.py`'s after a later onboarding round added 7 tickers only to those two files - fixed by syncing all three, and by flipping `blend.py`/`llm_news.py`/`quant_layer.py`/`eval/run_eval.py`/`build_cost_ledger.py`'s bare-CLI defaults from the retired 6 production issuers to the full phase2 set.
- **News-digest quarter-shift bug (2026-08-09 audit)**: 9 combos' `docs/news/<issuer>/<document_id>.txt` files actually contained the *next* quarter's (or two quarters later, for Nike) pre-earnings coverage - same failure signature as the NFLX transcript mislabeling above. All 4 Nvidia quarters and all 3 Nike quarters were affected (files shifted +1 and +2 slots respectively); fixed by re-filing the correctly-dated content into its real slot and sourcing fresh digests for the now-empty slots (`NVDA_FQ1_2025`, `NKE_FQ1_2025`, `NKE_FQ2_2025`). `DIS_FQ3_2025` and `GIS_FQ3_2026` were separately mislabeled/leaked (the latter contained an actual post-earnings EPS figure) and re-sourced clean. 4 more flagged combos (`SBUX_FQ3_2025`, `UNH_FQ4_2025`, `WDAY_FQ2_2025`, `DELL_FQ4_2025`) turned out to be false positives from the leakage checker matching a URL-embedded date string (a typo'd year, a Trefis "as-of" suffix, a forward-looking M&A close date, an inconsistent citation label) rather than real leaked content - left as-is. All 9 real fixes were rescored (`python llm_news.py`, ~$0.03 total) - **but since news carries 0.0 blend weight, none of this moved the deployed backtest numbers**; it matters for data hygiene and any future weighting that reintroduces news.
- **3 phantom issuers in `PHASE2_ISSUERS`** (`colgate_palmolive`, `costco`, `hermes` - listed in all three files' rosters but none had a `manifests/p2_*_reports.json`) meant a bare `python llm_news.py`/`blend.py`/`quant_layer.py` invocation would `FileNotFoundError` on the first of the three (alphabetically `colgate_palmolive`) and **silently skip every issuer after it** (~30 tickers) - it never bit the actual pipeline runs because `run_gap_pipeline.py` always passes explicit issuer args, but was a live landmine for the next bare rerun. Fixed 2026-08-09: built `manifests/p2_colgate_palmolive_reports.json` (4 quarters) and `manifests/p2_costco_reports.json` (3 quarters) from already-sourced docs that had been sitting unused in `docs/`; dropped `hermes` (no docs ever existed for it) from all three rosters; wrapped all three scripts' bare-CLI `for issuer in ...` loops in try/except so one bad/missing manifest can no longer kill the batch.
- **`blend.py`'s own `hold_upper`/`hold_lower` defaults were stale** (`derive_signal()` defaulted to symmetric `±0.25`, `blend_document()`/`blend_issuer()` defaulted to `±0.15` - neither matched the canonical `+0.25/-0.05`). Confirmed nothing downstream was reading these defaults (both `eval/run_eval.py` and `export_rows.py` already read the canonical CSV column directly), but fixed 2026-08-09 anyway: `DEFAULT_HOLD_UPPER`/`DEFAULT_HOLD_LOWER` now live in `blend.py` (not `eval/calibrate.py`, to avoid a circular import since `eval/calibrate.py` already imports from `blend.py`), and all three functions default to them. `eval/calibrate.py` now imports and re-exports the same two names, so every other file's `from eval.calibrate import DEFAULT_HOLD_UPPER, DEFAULT_HOLD_LOWER` still works unchanged.
- **`outputs/global/summary/backtest_equity_phase2.csv` was a stale orphaned artifact** (39 tickers, mtime 2026-07-30) that matched the project's `_phase2`-suffix naming convention closely enough to look current, while the real N=268 backtest output has always been the unsuffixed `backtest_equity.csv` (`backtest.py`'s actual default, no `_phase2` variant exists for that file). Removed via `git rm` 2026-08-09 (recoverable from git history) rather than left to trap the next reader.
- **`AMKBY_FQ4_2025` (Maersk) had `report_date: "2014-02-28"`** baked into `manifests/p2_maersk_reports.json`, `phase2/report_dates.json`, and the frozen `outputs/p2_maersk/results/AMKBY_FQ4_2025.json`'s `report_metadata.report_date` - fiscal period was correctly "FQ4 2025" and source docs correctly dated `CY2025-Q4`, but the date itself was a decade off. Missed by the 2026-08-09 audit's mismatch checker because this event apparently had no human-typed price pair to cross-validate against, so it never surfaced in `phase2/audit/report_date_audit.json`'s 33-combo mismatch list. Found incidentally 2026-08-10 while building the Tier 2 LM baseline - its chronological dev split showed 2014 as the earliest event date, which was the tell. The quant layer's own `outputs/quant/p2_maersk/results/AMKBY_FQ4_2025.json` had independently already matched `matched_earnings_date=2026-02-05` via its own earnings-calendar lookup (confirmed by `data/quantitative/sheet_export_cache/AMKBY_2026-02-05.csv` and the `price_cache/AMKBY_2025-11-07_2026-02-06.csv` window both existing), so 2026-02-05 is the real date, cross-confirmed rather than guessed. All three locations patched; `eval.run_eval --output-suffix phase2` and `backtest.py` rerun (see Current State for the updated headline numbers) - quant layer needed no rescore since it was already correct.
- **Report-date fixes require patching the scored result JSON's `report_metadata.report_date`, not just the manifest** - `eval/run_eval.py` reads `report_date` from each scored result's frozen `payload["report_metadata"]["report_date"]` (captured at scoring time), not live from `manifests/p2_*_reports.json` or `phase2/report_dates.json`. Editing only the manifest (as the 2026-08-09 report_date fixes initially did) has **no effect** on eval/backtest output until the corresponding `outputs/p2_<slug>/results/<document_id>.json` and `outputs/quant/p2_<slug>/results/<document_id>.json` are also patched (or the combo is rescored). No LLM cost to patch metadata directly since the underlying document text/analysis doesn't change - only the quant layer needs an actual (free, yfinance-only) rescore since its price-window calculations are date-dependent.

## Environment note

`C:\Users\bencr\OneDrive\Personal\Computing\Business Analytics\Citibank APR` (a path used earlier in this project) is a OneDrive-dependent alias that went fully inaccessible mid-session (OneDrive client process not running) - caused hours of apparent "hangs" reading/copying files before the cause was found. The real, stable project path is `C:\Users\bencr\Documents\Citibank APR` (same git repo) - use this one; it has no OneDrive dependency.
